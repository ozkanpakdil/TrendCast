---
phase: 01-data-reliability
plan: 02
type: tdd
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/unit/correlation-threshold.test.ts
autonomous: true
requirements:
  - REL-01
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A permanent diagnostic regression test asserts correlateNews confidence scores for Seeking Alpha / Investing.com-style headlines against sample market contracts WITHOUT changing the thresholds (D-01)."
    - "The test provides evidence for whether MIN_CONFIDENCE (0.75) / MIN_CONFIDENCE_ENTITY_MATCH (0.35) systematically drops these sources (D-03)."
  artifacts:
    - tests/unit/correlation-threshold.test.ts
  key_links:
    - "correlateNews() → confidence scores → asserted against MIN_CONFIDENCE / MIN_CONFIDENCE_ENTITY_MATCH"
---

<objective>
Add a permanent diagnostic regression test that feeds Seeking Alpha / Investing.com-style headlines against sample market contracts and asserts the resulting confidence scores against the existing correlation thresholds.

Purpose: This is the evidence-gathering mechanism for D-01/D-03. It does NOT change thresholds — it asserts scores so the planner/user can see whether `MIN_CONFIDENCE = 0.75` (or `MIN_CONFIDENCE_ENTITY_MATCH = 0.35` with entity match) systematically drops these sources. It acts as a regression guard for the REL-01 root cause.
Output: A passing diagnostic test that documents the confidence distribution for SA/Investing headlines.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-reliability/1-RESEARCH.md
@.planning/phases/01-data-reliability/01-PATTERNS.md
@.planning/phases/01-data-reliability/1-VALIDATION.md
@src/services/engine/correlation.ts
@src/utils/keywords.ts
@src/utils/entities.ts
@tests/unit/correlation.test.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add diagnostic regression test for SA/Investing correlation thresholds</name>
  <files>tests/unit/correlation-threshold.test.ts</files>
  <behavior>
    - Test 1: A Seeking Alpha-style headline sharing a named entity with a contract question produces a confidence score that is asserted (logged/compared) against MIN_CONFIDENCE (0.75) and MIN_CONFIDENCE_ENTITY_MATCH (0.35).
    - Test 2: An Investing.com-style headline sharing a keyword with a contract produces a confidence score asserted against the thresholds.
    - Test 3: A headline with no entity/keyword overlap produces zero matches (baseSim === 0 → null), documenting the drop-out path.
  </behavior>
  <action>
    Create `tests/unit/correlation-threshold.test.ts` (NEW), modeled on `tests/unit/correlation.test.ts` (Vitest `describe/it/expect`, `@/` path imports). Import `correlateNews` from `@/services/engine/correlation`, `extractKeywords` from `@/utils/keywords`, and types `NewsItem`, `MarketContract` from `@/types`.

    Build sample `MarketContract` fixtures (reuse the `mockContract` pattern from `correlation.test.ts` — e.g. "Will Bitcoin close above $100k on Dec 31?" with `keywords: ['bitcoin', 'btc', '100k', 'close', 'december']`).

    Build SA/Investing-style `NewsItem[]` fixtures with `source: 'seekingalpha'` / `source: 'investing'`, realistic financial headlines (e.g. "Bitcoin miners face margin squeeze as hash price falls"), and `keywords` arrays produced by `extractKeywords`.

    Assert the resulting `confidence` distribution:
    - For headlines sharing an entity with a contract, assert the confidence is compared against `MIN_CONFIDENCE_ENTITY_MATCH` (0.35) — the test documents whether the score clears the lower entity-match threshold.
    - For headlines sharing only keywords, assert the confidence is compared against `MIN_CONFIDENCE` (0.75).
    - For headlines with no overlap, assert `correlateNews` returns no match for that item (the `baseSim === 0` → null path).

    CRITICAL (D-01): Do NOT change `MIN_CONFIDENCE` or `MIN_CONFIDENCE_ENTITY_MATCH` in `src/services/engine/correlation.ts`. The test only asserts scores; it is the evidence mechanism for D-03, not a commitment to change thresholds. Use `expect(...).toBeGreaterThanOrEqual(...)` / `toBeLessThan(...)` against the existing constants to document whether scores clear them.

    Run the test — it MUST fail on first run (RED) because the assertions are written to reveal the actual score distribution, which may be below threshold. Commit the failing test.
  </files>
  <verify>
    <automated>bun run test -- --run tests/unit/correlation-threshold.test.ts</automated>
  </verify>
  <done>
    The diagnostic test exists, runs, and documents the SA/Investing confidence distribution against the unchanged thresholds. Thresholds in `correlation.ts` are unchanged (D-01).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → correlateNews | Sample headlines/contracts are fed into the correlation engine; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-03 | Tampering | `correlation-threshold.test.ts` | low | accept | Test-only file; no production code path. Thresholds remain unchanged (D-01). |
| T-1-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed in this phase. No legitimacy gate required. |
</threat_model>

<verification>
- `bun run test -- --run tests/unit/correlation-threshold.test.ts` passes (GREEN after implementation).
- `bun run test:all` passes at wave merge.
- `git diff src/services/engine/correlation.ts` shows no threshold changes (D-01 preserved).
</verification>

<success_criteria>
- A permanent diagnostic regression test asserts SA/Investing confidence scores against the unchanged thresholds.
- The test documents whether the thresholds systematically drop these sources (evidence for D-03).
- No threshold values changed.
</success_criteria>

<output>
Create `.planning/phases/01-data-reliability/01-02-SUMMARY.md` when done
</output>
