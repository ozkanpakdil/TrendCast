---
phase: 12-stock-indicator-end-to-end-wiring-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/popup/components/Settings.tsx
  - src/dashboard/components/NewsFeed.tsx
  - src/dashboard/components/HistoryChart.tsx
  - tests/e2e/fixtures.ts
  - tests/e2e/popup.spec.ts
  - tests/e2e/dashboard.spec.ts
autonomous: true
requirements:
  - SRC-04
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A user can toggle each of the three new sources (usaStocksIndicator, stockScreener, stockScreener2) on/off in the popup Settings Data Sources list."
    - "A user sees the three new sources labeled 'Stock Indicator', 'Breakout', and 'VCP' in the dashboard NewsFeed tiles."
    - "A user sees the three new sources colored teal, orange, and fuchsia in the NewsFeed tiles, with readable text (white on teal/fuchsia, dark on orange)."
    - "A user sees the three new sources labeled 'Stock Indicator', 'Breakout', and 'VCP' in the HistoryChart detail-panel news list."
    - "The three new sources are wired end-to-end (config, types, collector, background, dashboard, popup) — the popup toggle, NewsFeed, and HistoryChart all reference the same source keys."
  artifacts:
    - src/popup/components/Settings.tsx (3 new Data Sources toggle rows)
    - src/dashboard/components/NewsFeed.tsx (3 sourceLabels + 3 sourceColor palette entries + textColor branch)
    - src/dashboard/components/HistoryChart.tsx (3 PLATFORM_LABELS entries)
    - tests/e2e/fixtures.ts (GET_HISTORY mock reads from __store['trendcast:history'])
    - tests/e2e/popup.spec.ts (new-source toggle tests)
    - tests/e2e/dashboard.spec.ts (new-source label/color + HistoryChart detail-panel tests)
  key_links:
    - "Settings.tsx toggle key ↔ ExtensionSettings.enabledSources key (checkbox reads/writes the same key)"
    - "NewsFeed.tsx sourceLabels/sourceColor ↔ NewsItem.source (tile resolves label/color by source key)"
    - "HistoryChart.tsx PLATFORM_LABELS ↔ HistoryEntry.topNews[].source (detail-panel label)"
    - "Settings toggle label ↔ SourceHealthIndicator label (must match: 'Stock Indicator'/'Breakout'/'VCP')"
---

<objective>
Wire the three stock-indicator sources (`usaStocksIndicator`, `stockScreener`, `stockScreener2`) into the remaining UI surfaces so users can toggle each on/off in the popup settings and see them labeled/colored consistently across the dashboard.

Purpose: Phase 11 already added the three sources to the data layer (config, types, collector, background, alerts) and to `SourceHealthIndicator.tsx`. This phase completes the end-to-end wiring (SRC-04) by adding them to the popup settings toggle list, the dashboard `NewsFeed` label/color maps, and the `HistoryChart` detail-panel labels. The design system is fully established; this phase only extends existing source maps and the toggle list — no new components, tokens, or layout.

Output: The three new sources are toggleable in the popup and labeled/colored consistently across the dashboard, covered by e2e tests. No new npm packages.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stock-indicator-end-to-end-wiring-ui/12-UI-SPEC.md
@src/popup/components/Settings.tsx
@src/dashboard/components/NewsFeed.tsx
@src/dashboard/components/HistoryChart.tsx
@src/dashboard/components/SourceHealthIndicator.tsx
@src/types/index.ts
@tests/e2e/popup.spec.ts
@tests/e2e/dashboard.spec.ts
@tests/e2e/fixtures.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire usaStocksIndicator end-to-end across settings, NewsFeed, and HistoryChart</name>
  <files>src/popup/components/Settings.tsx, src/dashboard/components/NewsFeed.tsx, src/dashboard/components/HistoryChart.tsx, tests/e2e/fixtures.ts, tests/e2e/popup.spec.ts, tests/e2e/dashboard.spec.ts</files>
  <read_first>
    - src/popup/components/Settings.tsx (the Data Sources toggle array, ~line 150)
    - src/dashboard/components/NewsFeed.tsx (sourceLabels, sourceColor, textColor)
    - src/dashboard/components/HistoryChart.tsx (PLATFORM_LABELS, ~line 40)
    - src/dashboard/components/SourceHealthIndicator.tsx (sourceLabels — the label source of truth: 'Stock Indicator'/'Breakout'/'VCP')
    - tests/e2e/fixtures.ts (the cannedResponses object — GET_HISTORY currently returns hardcoded MOCK_HISTORY; GET_WATCHLIST reads from __store__)
    - tests/e2e/popup.spec.ts (openPopup + Settings tab test patterns)
    - tests/e2e/dashboard.spec.ts (openDashboard + News tab test patterns, override seeding)
    - tests/e2e/fixtures.ts (MOCK_SETTINGS, MOCK_SNAPSHOT, injectBrowserMock)
  </read_first>
  <action>
    Wire the single source `usaStocksIndicator` through all three UI surfaces end-to-end, proving the full wiring path for one source before adding the other two. This is the vertical slice.

    In `src/popup/components/Settings.tsx` (the Data Sources toggle array, currently `['polymarket', '🔵 Polymarket']` ... `['investing', '📰 Investing.com']`):
    - Add the row `['usaStocksIndicator', '📰 Stock Indicator']` as the last entry in the array (after `['investing', '📰 Investing.com']`). The array is typed `[keyof ExtensionSettings['enabledSources'], string][]`, so the key must match the existing `usaStocksIndicator` flag in `ExtensionSettings.enabledSources`.
    - Do NOT add `yahoo` or `googleFinance` — they are a pre-existing gap and out of scope per the UI-SPEC.

    In `src/dashboard/components/NewsFeed.tsx`:
    - Add `usaStocksIndicator: 'Stock Indicator'` to the `sourceLabels` Record (after the `investing` entry).
    - Add `usaStocksIndicator: [20, 184, 166]` (teal) to the `sourceColor` palette Record.
    - `textColor` needs no change for this source — it is not in the dark-text list, so it returns the default `'#ffffff'` (white on teal).

    In `src/dashboard/components/HistoryChart.tsx`:
    - Add `usaStocksIndicator: 'Stock Indicator'` to the `PLATFORM_LABELS` Record (after the `googleFinance` entry).

    In `tests/e2e/popup.spec.ts` (Settings tab describe block):
    - Add a test that seeds settings with `usaStocksIndicator: true` in `enabledSources` (via the `openPopup(page, { 'trendcast:settings': { ...MOCK_SETTINGS, enabledSources: { ...MOCK_SETTINGS.enabledSources, usaStocksIndicator: true } } })` override pattern), navigates to Settings, and asserts the `📰 Stock Indicator` toggle label is visible and its checkbox is checked.
    - Add a test that toggling the `📰 Stock Indicator` checkbox flips its checked state (following the existing `toggling a data source checkbox works` pattern).

    In `tests/e2e/dashboard.spec.ts` (News tab describe block):
    - Add a test that seeds a snapshot with one `usaStocksIndicator` news item (via the `openDashboard(page, { 'trendcast:latest-snapshot': { ...MOCK_SNAPSHOT, news: [ { id: 'news-usi', source: 'usaStocksIndicator', headline: 'Recent Tech Layoffs Stock Report', url: 'https://ozkanpakdil.github.io/usa-stocks-indicator/posts/layoffs/', publishedAt: new Date().toISOString(), keywords: ['layoffs'] } ] } })` override), navigates to the News tab, and asserts the `Stock Indicator` label renders on the tile. Also assert the tile's computed style: `toHaveCSS('background-color', 'rgb(20, 184, 166)')` (teal) and `toHaveCSS('color', 'rgb(255, 255, 255)')` (white text) — this makes the color truth executable (the `sourceColor`/`textColor` functions are module-private and not callable from e2e).
    - Extend the existing `Historical Trends` test (or add a new test) in the History tab describe block to seed the `'trendcast:history'` storage key (NOT the snapshot) with an array of AT LEAST 2 entries, where the LAST entry's `topNews` contains an item with `source: 'usaStocksIndicator'` (HistoryChart renders the detail panel only when `history.length >= 2`, and with no hover it defaults to the LAST entry via `detailIndex = hoverIndex ?? history.length - 1`). Assert the detail-panel news list renders the `Stock Indicator` label (proving the `PLATFORM_LABELS` entry is exercised, not just typechecked).
  </action>
  <verify>
    <automated>bun run typecheck && bun run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` passes (the `usaStocksIndicator` key is valid in the `enabledSources` union, `sourceLabels`, `sourceColor`, and `PLATFORM_LABELS`).
    - `bun run test:e2e` passes with the new popup and dashboard tests: the `📰 Stock Indicator` toggle renders checked and toggles; the NewsFeed tile shows the `Stock Indicator` label for a `usaStocksIndicator` item; the tile's computed background is `rgb(20, 184, 166)` and text color is `rgb(255, 255, 255)`; the HistoryChart detail panel renders the `Stock Indicator` label.
  </acceptance_criteria>
  <done>
    `usaStocksIndicator` is toggleable in the popup settings, labeled 'Stock Indicator' in the NewsFeed and HistoryChart, and colored teal with white text in the NewsFeed. The new e2e tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire stockScreener and stockScreener2 across all three UI surfaces</name>
  <files>src/popup/components/Settings.tsx, src/dashboard/components/NewsFeed.tsx, src/dashboard/components/HistoryChart.tsx, tests/e2e/fixtures.ts, tests/e2e/popup.spec.ts, tests/e2e/dashboard.spec.ts</files>
  <read_first>
    - src/popup/components/Settings.tsx (the Data Sources toggle array, ~line 150)
    - src/dashboard/components/NewsFeed.tsx (sourceLabels, sourceColor, textColor)
    - src/dashboard/components/HistoryChart.tsx (PLATFORM_LABELS, ~line 40)
    - src/dashboard/components/SourceHealthIndicator.tsx (sourceLabels — label source of truth)
    - tests/e2e/fixtures.ts (GET_HISTORY mock — now reads from __store__ after Task 1)
    - tests/e2e/popup.spec.ts (Settings tab test patterns)
    - tests/e2e/dashboard.spec.ts (News tab test patterns)
    - tests/e2e/fixtures.ts (MOCK_SETTINGS, MOCK_SNAPSHOT, injectBrowserMock)
  </read_first>
  <action>
    Expand the proven slice to the remaining two sources, `stockScreener` and `stockScreener2`, across the same three surfaces. Follow the exact patterns established in Task 1.

    In `src/popup/components/Settings.tsx` (Data Sources toggle array):
    - Add `['stockScreener', '📰 Breakout']` and `['stockScreener2', '📰 VCP']` as the last two entries (after the `usaStocksIndicator` row added in Task 1). Keys must match the existing `enabledSources` keys.

    In `src/dashboard/components/NewsFeed.tsx`:
    - Add `stockScreener: 'Breakout'` and `stockScreener2: 'VCP'` to `sourceLabels`.
    - Add `stockScreener: [249, 115, 22]` (orange) and `stockScreener2: [217, 70, 239]` (fuchsia) to the `sourceColor` palette.
    - Extend the `textColor` function so `stockScreener` (orange, a light hue) returns dark text `'#0f172a'` — add `|| source === 'stockScreener'` to the existing dark-text condition (`source === 'seekingalpha' || source === 'investing'`). `stockScreener2` (fuchsia, a dark hue) stays on the default `'#ffffff'` white text.

    In `src/dashboard/components/HistoryChart.tsx`:
    - Add `stockScreener: 'Breakout'` and `stockScreener2: 'VCP'` to `PLATFORM_LABELS`.

    In `tests/e2e/popup.spec.ts` (Settings tab describe block):
    - Add a test that seeds settings with `stockScreener: true` and `stockScreener2: true` in `enabledSources` (override pattern as in Task 1), navigates to Settings, and asserts the `📰 Breakout` and `📰 VCP` toggle labels are visible and checked.

    In `tests/e2e/dashboard.spec.ts` (News tab describe block):
    - Add a test that seeds a dashboard with one `stockScreener` item and one `stockScreener2` item (override pattern as in Task 1, with `source: 'stockScreener'` / `source: 'stockScreener2'` and distinct ids/headlines), navigates to the News tab, and asserts the `Breakout` and `VCP` labels render on the tiles. Also assert the computed styles: the `stockScreener` tile `toHaveCSS('background-color', 'rgb(249, 115, 22)')` (orange) and `toHaveCSS('color', 'rgb(15, 23, 42)')` (dark text `#0f172a`); the `stockScreener2` tile `toHaveCSS('background-color', 'rgb(217, 70, 239)')` (fuchsia) and `toHaveCSS('color', 'rgb(255, 255, 255)')` (white text).
    - Extend the HistoryChart detail-panel test (from Task 1) to also seed `topNews` entries with `source: 'stockScreener'` and `source: 'stockScreener2'` in the LAST entry of the `'trendcast:history'` array (still at least 2 entries; the detail panel defaults to the last entry), and assert the `Breakout` and `VCP` labels render in the detail-panel news list.
  </action>
  <verify>
    <automated>bun run typecheck && bun run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` passes (the `stockScreener`/`stockScreener2` keys are valid in `enabledSources`, `sourceLabels`, `sourceColor`, `textColor`, and `PLATFORM_LABELS`).
    - `bun run test:e2e` passes with the new popup and dashboard tests (the `📰 Breakout` and `📰 VCP` toggles render checked; the NewsFeed tiles render `Breakout` and `VCP` labels with computed backgrounds `rgb(249, 115, 22)` / `rgb(217, 70, 239)` and text colors `rgb(15, 23, 42)` / `rgb(255, 255, 255)`; the HistoryChart detail-panel renders the new labels).
  </acceptance_criteria>
  <done>
    `stockScreener` and `stockScreener2` are toggleable in the popup settings, labeled 'Breakout' and 'VCP' in the NewsFeed and HistoryChart, and colored orange (dark text) and fuchsia (white text) in the NewsFeed. The new e2e tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feed content → React rendering | Untrusted feed headlines/summaries are rendered in the NewsFeed tiles and HistoryChart detail panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | `NewsFeed` tile headline/summary rendering | low | mitigate | Headlines/summaries are rendered as React text (auto-escaped); no `dangerouslySetInnerHTML` in `NewsFeed.tsx` or `HistoryChart.tsx` — the new label/color maps only add static string keys, no new injection surface |
| T-12-02 | Tampering | `Settings.tsx` toggle labels | low | accept | Toggle labels are hardcoded string literals in the source array; no user-supplied input reaches them |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase — pure source-map + test additions; no package-legitimacy gate applies |
</threat_model>

<verification>
- `bun run typecheck` — confirms the new source keys are valid across `enabledSources`, `sourceLabels`, `sourceColor`, `textColor`, and `PLATFORM_LABELS`.
- `bun run test:e2e` — the new popup Settings toggle tests and dashboard News tab label tests pass, proving the sources render and toggle.
- Full suite `bun run test` (vitest unit) stays green — no unit tests are touched by this UI-only phase.
</verification>

<success_criteria>
- A user can toggle each of the three new sources on/off in the popup settings Data Sources list.
- The three new sources appear with correct labels ('Stock Indicator'/'Breakout'/'VCP') and colors (teal/orange/fuchsia) in the dashboard NewsFeed.
- The three new sources appear with correct labels in the HistoryChart detail-panel news list.
- The new sources are wired end-to-end (config, types, collector, background, dashboard, popup) — the popup toggle, NewsFeed, and HistoryChart all reference the same source keys.
- `SourceHealthIndicator` labels match the new toggle labels (already done in Phase 11 — verified, no change).
</success_criteria>

<output>
Create `.planning/phases/12-stock-indicator-end-to-end-wiring-ui/12-01-SUMMARY.md` when done.
</output>
