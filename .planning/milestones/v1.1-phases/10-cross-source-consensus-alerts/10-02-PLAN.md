---
phase: 10-cross-source-consensus-alerts
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/background/alerts.ts
  - tests/unit/cross-source-alerts.test.ts
autonomous: true
requirements: [PHASE-10]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Multiple posts from the same source type count once toward consensus (D-02).
    - A cluster with >=3 distinct source types but no social+news mix does NOT fire (D-01).
    - Cross-source alerts fire on any direction (bullish/bearish/mixed) (D-03).
    - Per-topic cooldown prevents re-alerting the same topic within the window (D-08).
    - Cross-source alerts are gated only by `alertsEnabled` (D-09).
  artifacts:
    - src/background/alerts.ts (hardened `evaluateCrossSourceAlerts` edge cases)
    - tests/unit/cross-source-alerts.test.ts (expanded coverage)
  key_links:
    - Distinct source-type counting dedupes by source type, not by post.
    - Social+news mixing check uses `SocialPlatform` vs `NewsSource` membership.
    - Cooldown keyed by `topicId` in `state.lastNotified`.
---

<objective>
Harden the cross-source consensus engine against edge cases and prove each locked decision with unit tests.

Purpose: The tracer (10-01) proved the happy path. This plan expands coverage to the counting, mixing, direction, cooldown, and gating rules (D-01, D-02, D-03, D-08, D-09) so the engine is deterministic and regression-safe.
Output: Edge-case fixes in `evaluateCrossSourceAlerts` plus an expanded `tests/unit/cross-source-alerts.test.ts`.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-cross-source-consensus-alerts/10-01-SUMMARY.md
@.planning/phases/10-cross-source-consensus-alerts/10-CONTEXT.md
@src/background/alerts.ts
@src/types/index.ts
@tests/unit/cross-source-alerts.test.ts
@tests/unit/alerts.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Distinct source-type counting + social/news mixing edge cases</name>
  <files>src/background/alerts.ts, tests/unit/cross-source-alerts.test.ts</files>
  <read_first>
    - src/background/alerts.ts §evaluateCrossSourceAlerts (the function created in 10-01)
    - src/types/index.ts §SocialPlatform (line ~56) and §NewsSource (line ~90)
    - tests/unit/cross-source-alerts.test.ts (the happy-path test from 10-01)
  </read_first>
  <behavior>
    - Test 1: A cluster with 3 posts from the SAME source type (e.g. 3 reddit signals) plus 1 news source yields a distinct source-type count of 2, so it does NOT fire (D-02 dedupe).
    - Test 2: A cluster with >=3 distinct source types but ALL social (e.g. x + reddit + tiktok, no news) does NOT fire (D-01 requires social+news mix).
    - Test 3: A cluster with >=3 distinct source types mixing social+news fires with `sourceTypes` containing exactly the distinct types, deduplicated.
  </behavior>
  <action>
    In `src/background/alerts.ts` §`evaluateCrossSourceAlerts`, ensure the distinct source-type set is built by deduplicating on the source-type string (from `match.signal.platform` for social and `match.news.source` for news), so multiple posts from the same source type count once (D-02). Ensure the consensus check requires BOTH: distinct count >= `CONFIG.alerts.minConsensusSourceTypes` AND at least one member of `SocialPlatform` AND at least one member of `NewsSource` in the set (D-01). Validate each source-type string against the typed unions before adding to the set; ignore unknown values (ASVS V5). Add the three tests from `<behavior>` to `tests/unit/cross-source-alerts.test.ts` using the existing storage-mock pattern and `newsSocialMatches` fixtures that share a common entity keyword.
  </action>
  <verify>
    <automated>bun run test -- tests/unit/cross-source-alerts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `evaluateCrossSourceAlerts` dedupes source types so 3 same-type posts + 1 news source yields count 2 and does not fire.
    - A social-only cluster (x+reddit+tiktok) does not fire.
    - A mixed cluster fires with `sourceTypes` deduplicated to the distinct types.
    - All three `<behavior>` tests pass in `tests/unit/cross-source-alerts.test.ts`.
  </acceptance_criteria>
  <done>Distinct source-type counting and social+news mixing rules are proven by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Any-direction firing, per-topic cooldown, and alertsEnabled gating</name>
  <files>src/background/alerts.ts, tests/unit/cross-source-alerts.test.ts</files>
  <read_first>
    - src/background/alerts.ts §evaluateCrossSourceAlerts and §evaluateAlerts cooldown block (lines ~151-166)
    - src/config/index.ts §CONFIG.alerts (globalCooldownMinutes, perMarketCooldownMinutes)
    - tests/unit/cross-source-alerts.test.ts
  </read_first>
  <behavior>
    - Test 1: A cluster whose mean sentiment is negative fires a `bearish` crossSource alert (D-03 any direction).
    - Test 2: A cluster whose mean sentiment is ~0 fires a `mixed` crossSource alert (D-03).
    - Test 3: Re-running `evaluateCrossSourceAlerts` for the same `topicId` within `perMarketCooldownMinutes` produces no new alert (D-08 per-topic cooldown).
    - Test 4: With `settings.alertsEnabled === false`, `evaluateCrossSourceAlerts` returns [] even with a consensus cluster (D-09).
  </behavior>
  <action>
    In `src/background/alerts.ts` §`evaluateCrossSourceAlerts`, confirm direction is derived from the mean of `match.signal.sentiment` across the cluster with NO directional filter (D-03): positive mean -> 'bullish', negative mean -> 'bearish', ~0 -> 'mixed'. Confirm the per-topic cooldown reads `state.lastNotified[topicId]` and compares against `settings.alertCooldownMinutes ?? CONFIG.alerts.perMarketCooldownMinutes`, and that the global cooldown uses `CONFIG.alerts.globalCooldownMinutes` (D-08). Confirm the function returns [] immediately when `!settings.alertsEnabled` (D-09). Add the four `<behavior>` tests to `tests/unit/cross-source-alerts.test.ts`, reusing the storage-mock pattern and controlling `now`/`alertedAt` to exercise the cooldown window.
  </action>
  <verify>
    <automated>bun run test -- tests/unit/cross-source-alerts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A negative-mean cluster yields a `bearish` crossSource alert; a ~0-mean cluster yields `mixed`.
    - Re-running within the per-topic cooldown window yields no new alert for the same `topicId`.
    - `alertsEnabled === false` returns [] regardless of consensus.
    - All four `<behavior>` tests pass.
  </acceptance_criteria>
  <done>Any-direction firing, per-topic cooldown, and alertsEnabled gating are proven by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored correlation result -> alert engine | `newsSocialMatches` source-type strings are validated against typed unions before counting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | source-type validation in `evaluateCrossSourceAlerts` | medium | mitigate | Reject unknown `platform`/`source` values before counting (ASVS V5 input validation). |
| T-10-05 | DoS | per-topic cooldown | low | mitigate | Cooldown keyed by `topicId` prevents hot-topic re-alert spam (D-08). |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced; existing devDependencies only. |
</threat_model>

<verification>
- `bun run test -- tests/unit/cross-source-alerts.test.ts` passes all edge-case tests.
- `bun run typecheck` exits 0.
</verification>

<success_criteria>
- All locked consensus rules (D-01, D-02, D-03, D-08, D-09) are covered by passing unit tests.
- The engine is deterministic and regression-safe.
</success_criteria>

<output>
Create `.planning/phases/10-cross-source-consensus-alerts/10-02-SUMMARY.md` when done
</output>
