---
phase: 10-cross-source-consensus-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/config/index.ts
  - src/background/alerts.ts
  - src/background/index.ts
  - src/dashboard/components/AlertsTab.tsx
  - tests/unit/cross-source-alerts.test.ts
autonomous: true
requirements: [PHASE-10]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - A topic appearing in >=3 distinct source types (mixing >=1 social + >=1 news) produces a crossSource alert even with an empty watchlist.
    - Cross-source alerts persist to the same unified alertHistory array as watchlist alerts.
    - The AlertsTab renders cross-source alerts as distinct cards and mentions them in the empty state.
  artifacts:
    - src/types/index.ts (AlertRecord kind discriminator + crossSource fields)
    - src/config/index.ts (minConsensusSourceTypes, requireSocialAndNews)
    - src/background/alerts.ts (evaluateCrossSourceAlerts)
    - src/background/index.ts (runAlertSweep hook)
    - src/dashboard/components/AlertsTab.tsx (cross-source card + empty state)
    - tests/unit/cross-source-alerts.test.ts
  key_links:
    - evaluateCrossSourceAlerts reads result.newsSocialMatches (not result.matches) and does NOT early-return on empty watchlist.
    - dispatchAlerts title falls back to topicLabel when question is undefined.
    - runAlertSweep combines watchlist + crossSource alerts before dispatch/broadcast.
---

<objective>
Deliver the end-to-end cross-source consensus alert path: a new `kind: 'crossSource'` alert that fires when the same topic appears across >=3 distinct source types (mixing social + news), even with an empty watchlist.

Purpose: Surface important topics the user is not watching by reusing the existing `newsSocialMatches` correlation output (D-06) and the shared alert notification/badge/history infrastructure (D-04). This is the tracer slice that proves the architecture end-to-end before expansion.
Output: `evaluateCrossSourceAlerts` engine, `AlertRecord` `kind` discriminator + crossSource fields, consensus config constants, `runAlertSweep` hook, `dispatchAlerts` title fallback, AlertsTab cross-source card + empty-state text, and a happy-path unit test.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-source-consensus-alerts/10-CONTEXT.md
@.planning/phases/10-cross-source-consensus-alerts/10-PATTERNS.md
@src/background/alerts.ts
@src/types/index.ts
@src/config/index.ts
@src/background/index.ts
@src/dashboard/components/AlertsTab.tsx
@src/services/engine/correlation.ts
@src/utils/entities.ts
@tests/unit/alerts.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end cross-source consensus alert engine — one path</name>
  <files>src/types/index.ts, src/config/index.ts, src/background/alerts.ts, src/background/index.ts, tests/unit/cross-source-alerts.test.ts</files>
  <read_first>
    - src/background/alerts.ts (full file — `evaluateAlerts`, `readAlertState`, `readAlertHistory`, `deriveDirection`, cooldown pattern, record construction)
    - src/types/index.ts §AlertRecord (lines ~407-428) and §Message union (lines ~460-505) for the discriminated-union pattern
    - src/config/index.ts §CONFIG.alerts (lines ~190-215) and §CONFIG.storage (lines ~120-121)
    - src/background/index.ts §runAlertSweep (lines ~274-290) and the `@/background/alerts` import (line ~55)
    - src/services/engine/correlation.ts §correlateNewsSocial (lines ~308-335) for the `NewsSocialCorrelationMatch` shape
    - src/utils/entities.ts §extractEntityKeywords (line ~360) and §extractEntities (line ~197)
    - tests/unit/alerts.test.ts (full file — the in-memory `browser.storage.local` mock pattern to mirror)
  </read_first>
  <action>
    Add a `kind: 'watchlist' | 'crossSource'` discriminator to `AlertRecord` in `src/types/index.ts` (D-04), following the `Message` discriminated-union pattern. Make `contractId`, `platform`, `question` optional (`contractId?: string`, `platform?: MarketPlatform`, `question?: string`) and add `topicLabel?: string` and `sourceTypes?: string[]` (D-05). Add `export type AlertKind = 'watchlist' | 'crossSource'`.

    Add consensus threshold constants to `CONFIG.alerts` in `src/config/index.ts` (D-01): `minConsensusSourceTypes: 3` and `requireSocialAndNews: true`.

    In `src/background/alerts.ts`, add `export async function evaluateCrossSourceAlerts(result: CorrelationResult, settings: ExtensionSettings, now: number = Date.now()): Promise<AlertRecord[]>`. It MUST NOT early-return on empty watchlist (it takes no watchlist argument). Gate on `if (!settings.alertsEnabled) return []` (D-09). Read `result.newsSocialMatches` (D-06); if empty return []. Read `[state, history]` via the existing `readAlertState()`/`readAlertHistory()` helpers. Cluster matches by shared entities/keywords using `extractEntityKeywords` from `@/utils/entities` (D-07): for each match, union `extractEntityKeywords(match.news.headline)` and `extractEntityKeywords(match.signal.text)`; group matches sharing >=1 normalized keyword into a cluster keyed by a stable `topicId` (the most frequent normalized keyword). For each cluster, count distinct source types from `match.signal.platform` (x/reddit/tiktok) and `match.news.source` (bbc/cnn/yahoo/googleFinance/seekingalpha/investing) (D-02); a cluster fires only when the distinct count is >= `CONFIG.alerts.minConsensusSourceTypes` AND it contains >=1 social platform AND >=1 news source (D-01). Derive aggregate direction from the mean of `match.signal.sentiment` across the cluster (D-03, any direction fires; mixed when the mean is ~0). Apply the global cooldown (`CONFIG.alerts.globalCooldownMinutes`) and a per-topic cooldown keyed by `topicId` using `state.lastNotified[topicId]` and `settings.alertCooldownMinutes ?? CONFIG.alerts.perMarketCooldownMinutes` (D-08). Build the record with `kind: 'crossSource'`, `id: \`${topicId}:${now}\``, `topicLabel` (the most frequent normalized keyword, humanized), `sourceTypes` (the distinct source-type array), `direction`, `sentiment` (mean), `yesPrice: 0`, `topSignalText` (highest-confidence match's `signal.text`), `topNewsHeadline` (highest-confidence match's `news.headline`), `confidence` (max match confidence), `alertedAt: now`. Persist to the SAME `CONFIG.storage.alertState`/`alertHistory` keys (D-04 unified history), capping with `.slice(-CONFIG.alerts.historyCap)`. Return the new records.

    In `src/background/index.ts` §`runAlertSweep`, after `const newAlerts = await evaluateAlerts(result, watchlist, settings)`, add `const crossSourceAlerts = await evaluateCrossSourceAlerts(result, settings)` and combine `const allAlerts = [...newAlerts, ...crossSourceAlerts]`; dispatch/broadcast when `allAlerts.length > 0`. Add `evaluateCrossSourceAlerts` to the existing `@/background/alerts` import.

    Create `tests/unit/cross-source-alerts.test.ts` mirroring the `alerts.test.ts` storage-mock pattern. Build a `CorrelationResult` whose `newsSocialMatches` span >=3 distinct source types (e.g. reddit + x social, bbc + cnn + seekingalpha news) sharing a common entity keyword (e.g. 'bitcoin'), pass an EMPTY watchlist, and assert `evaluateCrossSourceAlerts` returns a record with `kind === 'crossSource'`, `topicLabel` set, `sourceTypes.length >= 3`, and that it persists to `alertHistory`.
  </action>
  <verify>
    <automated>bun run test -- tests/unit/cross-source-alerts.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/index.ts` exports `AlertKind` and `AlertRecord` has `kind: AlertKind`, optional `contractId`/`platform`/`question`, and `topicLabel?`/`sourceTypes?`.
    - `src/config/index.ts` `CONFIG.alerts` contains `minConsensusSourceTypes: 3` and `requireSocialAndNews: true`.
    - `src/background/alerts.ts` exports `evaluateCrossSourceAlerts(result, settings, now?)` that reads `result.newsSocialMatches` and does not reference a watchlist.
    - `src/background/index.ts` `runAlertSweep` calls `evaluateCrossSourceAlerts` and combines its output with `evaluateAlerts` output before dispatch/broadcast.
    - `tests/unit/cross-source-alerts.test.ts` passes: an empty-watchlist `CorrelationResult` with >=3 distinct source types sharing a keyword yields a `kind === 'crossSource'` record persisted to `alertHistory`.
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>The single cross-source happy path works end-to-end (newsSocialMatches -> cluster -> consensus -> crossSource record -> unified history) and is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-source notification title fallback + AlertsTab rendering</name>
  <files>src/background/alerts.ts, src/dashboard/components/AlertsTab.tsx</files>
  <read_first>
    - src/background/alerts.ts §dispatchAlerts (lines ~250-320) — the notification id/title construction that assumes `record.contractId`/`record.question`
    - src/dashboard/components/AlertsTab.tsx (full file) — empty-state block (lines ~73-80) and card renderer (lines ~104-130)
  </read_first>
  <action>
    In `src/background/alerts.ts` §`dispatchAlerts`, make the notification id and title safe for crossSource records where `contractId`/`question` are undefined (D-05): id -> `trendcast-alert-${record.contractId ?? record.topicLabel ?? 'cross'}-${record.alertedAt}`; title -> `${record.direction} — ${record.question ?? record.topicLabel ?? 'Cross-source alert'}`.

    In `src/dashboard/components/AlertsTab.tsx`, update the empty-state message (D-10) to: "Alerts appear here when a watchlisted market moves, or when a topic gains consensus across multiple sources." Add a `kind === 'crossSource'` branch in the card renderer (D-11): render `alert.topicLabel` instead of `alert.question` as the title, add a "Cross-source" badge (e.g. `bg-indigo-900/50 text-indigo-300` within the UI-SPEC color contract), and render the source breakdown from `alert.sourceTypes` joined with " · " (e.g. "X · Reddit · Seeking Alpha"). Watchlist cards (`kind === 'watchlist'` or undefined) render exactly as today.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `dispatchAlerts` builds a notification id and title that never contain the literal string "undefined" for a crossSource record (falls back to `topicLabel`).
    - `AlertsTab.tsx` empty-state text contains the substring "topic gains consensus across multiple sources".
    - `AlertsTab.tsx` renders a distinct card for `kind === 'crossSource'` showing `topicLabel`, a "Cross-source" badge, and `sourceTypes` joined with " · ".
    - Watchlist cards (`kind === 'watchlist'` or undefined) render `question` as before.
    - `bun run typecheck` and `bun run lint` both exit 0.
  </acceptance_criteria>
  <done>Cross-source alerts render as distinct cards with a safe notification title, and the empty state mentions both alert kinds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored correlation result -> alert engine | `result.newsSocialMatches` is read from `chrome.storage.local`; source-type strings are validated against the typed unions before counting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | `evaluateCrossSourceAlerts` source-type counting | medium | mitigate | Validate `match.signal.platform` against `SocialPlatform` and `match.news.source` against `NewsSource` before counting; ignore unknown values (ASVS V5 input validation). |
| T-10-02 | Spoofing | `dispatchAlerts` notification title | low | mitigate | Fall back to `topicLabel` when `question` is undefined so crossSource notifications never show "undefined". |
| T-10-03 | DoS | per-topic cooldown keyed by `topicId` | low | mitigate | Reuse `state.lastNotified[topicId]` + `perMarketCooldownMinutes` so a hot topic cannot re-alert within the cooldown window (D-08). |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced in this phase; existing devDependencies only. |
</threat_model>

<verification>
- `bun run test -- tests/unit/cross-source-alerts.test.ts` passes (happy path).
- `bun run typecheck` exits 0.
- `bun run lint` exits 0.
</verification>

<success_criteria>
- A crossSource alert fires end-to-end from `newsSocialMatches` with an empty watchlist.
- Cross-source records share the unified `alertHistory` with watchlist records.
- AlertsTab renders cross-source cards distinctly and the empty state mentions both kinds.
</success_criteria>

<output>
Create `.planning/phases/10-cross-source-consensus-alerts/10-01-SUMMARY.md` when done
</output>
