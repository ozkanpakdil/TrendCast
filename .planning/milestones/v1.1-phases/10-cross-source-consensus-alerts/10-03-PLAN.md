---
phase: 10-cross-source-consensus-alerts
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/dashboard/components/AlertsTab.tsx
  - src/dashboard/hooks/useAlerts.ts
  - tests/unit/cross-source-alerts.test.ts
autonomous: true
requirements: [PHASE-10]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Cross-source alert cards render distinctly with topic label, "Cross-source" badge, and source breakdown (D-11).
    - The AlertsTab empty state mentions both watchlist and cross-source alerts (D-10).
    - Watchlist cards render unchanged.
    - The full unit suite passes with no regressions.
  artifacts:
    - src/dashboard/components/AlertsTab.tsx (cross-source card + empty state)
    - src/dashboard/hooks/useAlerts.ts (no change expected — verified)
    - tests/unit/cross-source-alerts.test.ts (UI-render assertions)
  key_links:
    - `kind === 'crossSource'` branch renders `topicLabel` + source breakdown; watchlist branch renders `question`.
    - `useAlerts` passes `kind` through transparently via the shared `alertHistory`/`ALERTS_UPDATED`.
---

<objective>
Finalize the AlertsTab cross-source UI (distinct card + empty-state copy) and run full regression verification.

Purpose: Complete the user-facing surface for cross-source alerts (D-10, D-11) and prove the whole phase integrates cleanly with the existing alert infrastructure and test suite.
Output: Kind-aware AlertsTab rendering, updated empty-state text, and a green full-suite run.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-cross-source-consensus-alerts/10-01-SUMMARY.md
@.planning/phases/10-cross-source-consensus-alerts/10-02-SUMMARY.md
@.planning/phases/10-cross-source-consensus-alerts/10-CONTEXT.md
@src/dashboard/components/AlertsTab.tsx
@src/dashboard/hooks/useAlerts.ts
@tests/unit/cross-source-alerts.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kind-aware AlertsTab card rendering + empty-state copy</name>
  <files>src/dashboard/components/AlertsTab.tsx</files>
  <read_first>
    - src/dashboard/components/AlertsTab.tsx (full file — empty-state block lines ~73-80, card renderer lines ~104-130, `directionBadges` map)
    - src/types/index.ts §AlertRecord (the `kind`/`topicLabel`/`sourceTypes` fields added in 10-01)
  </read_first>
  <action>
    In `src/dashboard/components/AlertsTab.tsx`, update the empty-state message (D-10) to: "Alerts appear here when a watchlisted market moves, or when a topic gains consensus across multiple sources." In the card renderer, branch on `alert.kind === 'crossSource'`: render `alert.topicLabel` as the title (falling back to `alert.question` if `topicLabel` is absent), add a "Cross-source" badge (e.g. `bg-indigo-900/50 text-indigo-300` within the UI-SPEC color contract), and render the source breakdown from `alert.sourceTypes` joined with " · " (e.g. "X · Reddit · Seeking Alpha") as a muted line under the title. Watchlist cards (`kind === 'watchlist'` or undefined) render exactly as today, using `alert.question`. Keep the existing direction badge and relative-time display for both kinds.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `AlertsTab.tsx` empty-state text contains the substring "topic gains consensus across multiple sources".
    - A `kind === 'crossSource'` record renders `topicLabel` as the title, a "Cross-source" badge, and `sourceTypes` joined with " · ".
    - A `kind === 'watchlist'` (or undefined) record renders `question` as before.
    - `bun run typecheck` and `bun run lint` both exit 0.
  </acceptance_criteria>
  <done>AlertsTab renders cross-source cards distinctly and the empty state mentions both alert kinds.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression verification of the phase</name>
  <files>tests/unit/cross-source-alerts.test.ts</files>
  <read_first>
    - tests/unit/cross-source-alerts.test.ts (all tests from 10-01 and 10-02)
    - tests/unit/alerts.test.ts (existing watchlist-alert tests that must not regress)
  </read_first>
  <action>
    Verify `useAlerts.ts` needs no change: it already loads `alertHistory` and listens for `ALERTS_UPDATED`, so the `kind` field flows through transparently (D-04). If any type error surfaces from the `AlertRecord` optional-field change, fix the consumer (e.g. `AlertsTab` or `dispatchAlerts`) rather than reverting the type. Run the full unit suite and the typecheck to confirm no regressions in the existing watchlist-alert tests (`tests/unit/alerts.test.ts`) or the correlation tests.
  </action>
  <verify>
    <automated>bun run test && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test` passes the full unit suite (including `tests/unit/alerts.test.ts` and `tests/unit/cross-source-alerts.test.ts`).
    - `bun run typecheck` exits 0.
    - `useAlerts.ts` is unchanged (no modification needed).
  </acceptance_criteria>
  <done>The full unit suite and typecheck pass with no regressions; the phase integrates cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alert history -> AlertsTab | `AlertRecord` fields rendered in the UI; `topicLabel`/`sourceTypes` are display-only strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Information Disclosure | AlertsTab rendering of `sourceTypes` | low | accept | Source-type names are non-sensitive public identifiers (x/reddit/bbc/etc.); no user data exposed. |
| T-10-07 | Tampering | `AlertRecord` optional-field consumers | medium | mitigate | Fix any consumer type error from the optional `contractId`/`question` change rather than reverting the type (D-05). |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced; existing devDependencies only. |
</threat_model>

<verification>
- `bun run test` passes the full unit suite.
- `bun run typecheck` exits 0.
- `bun run lint` exits 0.
</verification>

<success_criteria>
- Cross-source alerts render distinctly in the AlertsTab with topic label, "Cross-source" badge, and source breakdown.
- The empty state mentions both watchlist and cross-source alerts.
- The full unit suite and typecheck pass with no regressions.
</success_criteria>

<output>
Create `.planning/phases/10-cross-source-consensus-alerts/10-03-SUMMARY.md` when done
</output>
