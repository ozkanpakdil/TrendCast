---
phase: 14-ticker-cashtag-bridging
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/services/collectors/news.ts
  - src/utils/source-health.ts
  - src/dashboard/App.tsx
  - src/dashboard/components/SourceHealthIndicator.tsx
  - tests/unit/news-collector.test.ts
  - tests/unit/source-health.test.ts
autonomous: true
requirements: [CORR-03, CORR-04]
estimate:
  tokens: 48000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A stock-indicator NewsItem's keywords are exactly its lowercase ticker (e.g. ['amzn']) — source-label tokens ('stock', 'indicator', 'breakout', 'vcp') and date tokens never appear (CORR-03)"
    - "Non-stock-indicator sources keep raw extractKeywords(fullText) output — curation is scoped to the three STOCK_INDICATOR_SOURCES only"
    - "computeBridgingCoverage groups accumulated news by source and counts items whose id appears in newsMatches — empty news or empty matches yield an empty/partial map with no NaN (CORR-04)"
    - "Bridging coverage renders as 'bridged X/Y' in the SourceHealthIndicator tooltip at both App.tsx render sites, with 0/0 shown as 0 not NaN"
    - "The bridged end-to-end match from Plan 14-01 still passes after curation — curation narrows the keyword set but the ticker token survives"
  artifacts:
    - "src/services/collectors/news.ts — ticker-only keyword curation in the stock-indicator block"
    - "src/utils/source-health.ts — exported computeBridgingCoverage(news, newsMatches) pure projection"
    - "src/dashboard/components/SourceHealthIndicator.tsx — bridgingCoverage prop + tooltip segment"
    - "src/dashboard/App.tsx — bridgingCoverage wired at both SourceHealthIndicator render sites"
    - "tests/unit/source-health.test.ts — new computeBridgingCoverage describe block"
    - "tests/unit/news-collector.test.ts — curation assertions for the three stock-indicator sources"
  key_links:
    - "news.ts curation → engine keywordSimilarity: curated ['amzn'] intersects a $AMZN signal's bare ['amzn'] (Plan 14-01) with Jaccard 1.0 instead of being diluted by label tokens"
    - "App.tsx correlations?.newsMatches → computeBridgingCoverage → SourceHealthIndicator tooltip — same data flow as the existing correlatedCounts prop"
    - "computeBridgingCoverage is a pure function of (news, newsMatches) — no storage schema change, no background write path"
---

<objective>
Curate stock-indicator item keywords down to ticker tokens (dropping screener-label noise that dilutes Jaccard denominators) and surface bridging health: a pure `computeBridgingCoverage` projection in `source-health.ts` displayed per-source in `SourceHealthIndicator`.

Purpose: CORR-03 + CORR-04 — after Plan 14-01's form bridging, a 6-keyword news set sharing 1 ticker with a 2-keyword signal still scores low because `Stock Indicator`/`Breakout`/`VCP` label words pollute the keyword set; and the user has no way to see how many collected stock-indicator items actually produced a correlation.

Output: Curated keywords in `news.ts`, `computeBridgingCoverage` in `source-health.ts`, coverage display in `SourceHealthIndicator.tsx` + `App.tsx`, and unit tests for both.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ticker-cashtag-bridging/14-RESEARCH.md
@.planning/phases/14-ticker-cashtag-bridging/14-PATTERNS.md
@.planning/phases/14-ticker-cashtag-bridging/14-01-SUMMARY.md

@src/services/collectors/news.ts
@src/utils/source-health.ts
@src/dashboard/components/SourceHealthIndicator.tsx
@src/dashboard/App.tsx
@tests/unit/news-collector.test.ts
@tests/unit/source-health.test.ts
</context>

<artifacts_this_phase_produces>
Symbols and files this phase (both plans) creates or materially changes:

**Plan 14-01 (sibling plan, Wave 1):**
- `isKnownTicker(token: string): boolean` — new export in `src/utils/entities.ts`
- `TICKER_TO_ORG` map + bare-caps ticker recognition stage in `src/utils/entities.ts`
- Bare cashtag emission in `extractKeywords`, strip-$ bridge in `keywordSimilarity`
- Boost-detection rework in `src/services/engine/correlation.ts`; `humanizeTopic` parity in `src/background/alerts.ts`
- `tests/unit/bridging.test.ts` — new test file

**Plan 14-02 (this plan):**
- `computeBridgingCoverage(news: NewsItem[], newsMatches: NewsCorrelationMatch[]): Partial<Record<NewsSource, { total: number; bridged: number }>>` — new export in `src/utils/source-health.ts`
- `bridgingCoverage` prop on `SourceHealthIndicatorProps` in `src/dashboard/components/SourceHealthIndicator.tsx`
- Ticker-only keyword curation for `STOCK_INDICATOR_SOURCES` in `src/services/collectors/news.ts`
- `bridgingCoverage={computeBridgingCoverage(...)}` at both App.tsx render sites
- New `describe('computeBridgingCoverage')` block in `tests/unit/source-health.test.ts`; curation assertions in `tests/unit/news-collector.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Curate stock-indicator keywords to ticker tokens (CORR-03)</name>
  <files>src/services/collectors/news.ts, tests/unit/news-collector.test.ts</files>
  <read_first>
    - src/services/collectors/news.ts (the file being modified — STOCK_INDICATOR_SOURCES set, STOCK_SOURCE_LABELS, and the stock-indicator block inside collectFromSource's flatMap where `keywords: extractKeywords(...)` is emitted)
    - tests/unit/news-collector.test.ts (the `expands a Stock Indicator report…` and `expands a screener feed…` tests that assert current keyword contents)
    - .planning/phases/14-ticker-cashtag-bridging/14-PATTERNS.md (news.ts curation pattern: ticker-only tokens per research A4)
    - .planning/phases/14-ticker-cashtag-bridging/14-01-SUMMARY.md (bare keyword forms the curated tokens must match)
  </read_first>
  <action>
    In the stock-indicator branch of `collectFromSource` (the `symbols.map(...)` return inside the `STOCK_INDICATOR_SOURCES.has(source)` block), replace the raw `extractKeywords(`${stockHeadline} ${symbol}`)` keyword emission with ticker-only tokens: the lowercased symbol alone (research A4 — ticker-only, no org aliases; org-name bridging is handled by the unified entity space from Plan 14-01). Keep the existing explanatory comment style, updating it to state that keywords are curated to the bare ticker so label tokens cannot dilute keyword Jaccard (CORR-03). The headline text, id derivation, summary omission, category classification, and the non-stock-indicator return path (`keywords: extractKeywords(fullText)`) stay untouched.

    In `tests/unit/news-collector.test.ts`, update the existing keyword assertions in the stock-indicator describe block: the AMZN item's keywords must be exactly `['amzn']` (not merely contain it), and the XPON item's keywords must be exactly `['xpon']`. Add assertions that the label tokens `stock`, `indicator`, `breakout`, and `vcp` appear in NO stock-indicator item's keywords, and that a date-bearing title (`2026-08-23`) contributes no keyword. Add one assertion that a BBC item still carries raw extractKeywords output (curation scoping).
  </action>
  <verify>
    <automated>bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test` exits 0 with the updated news-collector assertions green
    - For a mocked usaStocksIndicator feed listing AMZN, the collected item's `keywords` deep-equals `['amzn']` (exact array, not toContain)
    - For a mocked stockScreener feed listing XPON/GENB/OABI, each item's `keywords` deep-equals its single lowercased ticker
    - No stock-indicator item's keywords contain `stock`, `indicator`, `breakout`, or `vcp` (asserted)
    - A bbc item's keywords still derive from `extractKeywords(fullText)` (asserted — curation did not leak to other sources)
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Stock-indicator items carry exactly one keyword — their bare lowercase ticker — and no screener-label or date noise; all other sources unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">Pure collection-time transform; reverting restores raw extractKeywords output with no stored-state dependency.</reversibility>
  <precondition>Plan 14-01 merged: extractKeywords emits bare forms so the curated token `amzn` and a $AMZN signal's keyword intersect in keywordSimilarity.</precondition>
</task>

<task type="auto">
  <name>Task 2: Bridging coverage projection + SourceHealthIndicator display (CORR-04)</name>
  <files>src/utils/source-health.ts, src/dashboard/components/SourceHealthIndicator.tsx, src/dashboard/App.tsx, tests/unit/source-health.test.ts</files>
  <read_first>
    - src/utils/source-health.ts (the file being modified — computeCorrelatedCounts and computeFetchedCounts are the literal templates; note the import block and the file's pure-projection docstring convention)
    - src/dashboard/components/SourceHealthIndicator.tsx (the file being modified — SourceHealthIndicatorProps, the correlatedCounts consumption, and the tooltip title template)
    - src/dashboard/App.tsx (both SourceHealthIndicator render sites passing correlatedCounts — around the news-tab aside and the correlations-tab aside)
    - tests/unit/source-health.test.ts (the computeCorrelatedCounts describe block: local match() builder, empty-input case, grouping case — the template for the new block)
    - src/types/index.ts (NewsCorrelationMatch shape: contract, news, confidence, matchedKeywords, correlatedAt)
  </read_first>
  <action>
    In `src/utils/source-health.ts`, add `computeBridgingCoverage(news: NewsItem[], newsMatches: NewsCorrelationMatch[]): Partial<Record<NewsSource, { total: number; bridged: number }>>` directly after `computeFetchedCounts`, following the file's pure-projection convention: build a Set of matched news ids from newsMatches, then fold the news array into a per-source accumulator `{ total, bridged }`, incrementing `bridged` when the item's id is in the matched set. No storage I/O, no React imports, no SourceHealthEntry schema change. Docstring states the CORR-04 semantics: fraction of a source's collected items that produced at least one correlation match.

    In `src/dashboard/components/SourceHealthIndicator.tsx`, add an optional `bridgingCoverage?: Partial<Record<NewsSource, { total: number; bridged: number }>>` prop to SourceHealthIndicatorProps (optional so existing usages stay valid), destructure it in the impl, and append a tooltip segment after the correlated count: when a coverage entry exists for the source, render ` · bridged B/T` using the entry's bridged and total; when the entry is absent or total is 0, render ` · bridged 0/0` — never a NaN or undefined interpolation. Keep the existing tooltip segments and click-filter behavior byte-identical otherwise.

    In `src/dashboard/App.tsx`, extend the import from `@/utils/source-health` with `computeBridgingCoverage` and pass `bridgingCoverage={computeBridgingCoverage(snapshot?.news ?? [], correlations?.newsMatches ?? [])}` at BOTH SourceHealthIndicator render sites (news tab aside and correlations tab aside), mirroring the existing correlatedCounts lines.

    In `tests/unit/source-health.test.ts`, add a `describe('computeBridgingCoverage')` block modeled on the computeCorrelatedCounts block: reuse the existing `item(source, id)` builder style for news and the `match(source)` builder style for matches (with the match's news id aligned to a real item id). Cases: empty news and empty matches → empty object; items with no matches → every source `{ total: N, bridged: 0 }`; mixed bridged/unbridged items per source count correctly; a match whose news id is not in the news array is ignored (no phantom source entries); duplicate matches for one item count it once (Set semantics); a single item bridged → `{ total: 1, bridged: 1 }` (single-element boundary).
  </action>
  <verify>
    <automated>bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test` exits 0 with the new computeBridgingCoverage describe block green
    - `computeBridgingCoverage([], [])` returns `{}`; items without matches yield `{ total: N, bridged: 0 }` per source; a match for an unknown news id adds no entry (all asserted in tests/unit/source-health.test.ts)
    - Duplicate matches for the same item id count once (Set-based bridgedIds, asserted)
    - `grep -c "bridgingCoverage" src/dashboard/App.tsx` returns 2 (both render sites wired)
    - SourceHealthIndicator tooltip renders `bridged 0/0` (not NaN/undefined) when coverage is absent or total is 0 — the pure-function contract (empty/absent/zero cases) is asserted in tests/unit/source-health.test.ts, and the rendered tooltip is verified manually per 14-VALIDATION.md (no component test exists or is planned for CORR-04 rendering)
    - `bun run typecheck` and `bun run lint` exit 0
  </acceptance_criteria>
  <done>
    Bridging coverage is computable as a pure projection, wired into both dashboard render sites, and displayed per-source as `bridged X/Y` in the health tooltip with a defined 0/0 case.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive projection + optional prop; removing it restores the prior tooltip with no data or schema impact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSS screener tables → extractStockSymbols → curated keywords | Hostile feed HTML flows into symbol extraction feeding the curated keyword array |
| correlations?.newsMatches (stored) → computeBridgingCoverage | Persisted match arrays re-enter the UI projection at render time |

## Schema Push Detection

Scan result: NO ORM/schema files in phase scope (no prisma/knex/migrations/SQL). `SourceHealthEntry` schema is explicitly unchanged — coverage is a render-time pure projection over data already in memory (research Open Question 3 recommendation). No blocking schema task needed.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | DoS | computeBridgingCoverage over large news arrays | low | accept | O(news + matches) single pass with a Set lookup; news arrays are already bounded by the storage budget (BUDGET_KEYS). Runs at render over data already in memory — same cost class as the existing computeFetchedCounts call. |
| T-14-05 | Tampering | curated keywords from hostile symbol extraction | low | accept | Symbols come from the existing extractStockSymbols regexes (uppercase-letter-bounded, ≤10 chars); curation emits only the lowercased symbol, so hostile content cannot inject arbitrary keyword strings beyond what symbol extraction already accepted. |
| T-14-06 | Information disclosure (storage growth) | curated keyword arrays | low | accept | Curation SHRINKS stored keyword arrays (6+ tokens → 1) for the three stock-indicator sources; no new storage keys. |
| T-14-SC | Tampering | package installs | high | mitigate | N/A this phase — zero new dependencies (Package Legitimacy Audit table empty; no install task exists). |
</threat_model>

<verification>
- `bun run test` — full unit suite green including new coverage tests and updated collector assertions
- `bun run typecheck` and `bun run lint` — clean
- Manual (from 14-VALIDATION.md): open dashboard source health after collection; stock-indicator sources show `bridged X/Y` in tooltip
</verification>

<success_criteria>
- Stock-indicator item keywords are exactly the bare ticker — no `stock`/`indicator`/`breakout`/`vcp`/date tokens
- Bridged matches survive curation (ticker token is the sole keyword and it bridges via Plan 14-01)
- Per-source bridging coverage (bridged/total) is visible in the source-health tooltip at both render sites, with defined empty/zero behavior
</success_criteria>

<output>
Create `.planning/phases/14-ticker-cashtag-bridging/14-02-SUMMARY.md` when done
</output>