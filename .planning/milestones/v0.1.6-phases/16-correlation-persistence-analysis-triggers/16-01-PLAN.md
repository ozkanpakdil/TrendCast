---
phase: 16-correlation-persistence-analysis-triggers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/utils/correlation-persistence.ts
  - src/background/index.ts
  - src/dashboard/hooks/useCorrelations.ts
  - src/dashboard/App.tsx
  - tests/unit/correlation-persistence.test.ts
  - tests/e2e/fixtures.ts
  - tests/e2e/dashboard.spec.ts
autonomous: true
requirements:
  - TRIG-01
  - TRIG-02
  - TRIG-04
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every terminal correlation run path (success, ML error, cancel, precompute success/error/throw, SW-death recovery) persists the result to CONFIG.storage.correlations with computedAt (epoch ms) plus engine, and model/inputCounts where in scope."
    - "An error result never overwrites a stored non-error result; the CORRELATION_RESULT broadcast still fires so the active run's UI settles."
    - "A persisted error result is treated as 'no analysis' by the auto-run gate — it never suppresses future auto-analysis."
    - "Opening the dashboard with a stored non-error result renders it instantly and sends zero CORRELATE_ALL messages."
    - "Opening the dashboard with no stored result (or an error-only result) and snapshot data present auto-runs analysis exactly once per dashboard session."
    - "The Correlations header shows computedAt as relative time plus the engine from the stored result; legacy results without computedAt show 'unknown age'; results older than the last collection show a stale marker."
    - "Corrupt, absent, or wrong-shaped stored correlation data degrades to 'no analysis' — nothing is thrown into the UI."
  artifacts:
    - "src/utils/correlation-persistence.ts (NEW — stampCorrelationResult, persistCorrelationResult, hasFreshAnalysis, readStoredAnalysis, CorrelationInputCounts, CorrelationStampMeta)"
    - "src/types/index.ts (CorrelationResult gains optional computedAt, model, inputCounts fields)"
    - "src/background/index.ts (all six terminal write sites routed through persistCorrelationResult; runCorrelationPrecompute gains a local try/catch and a CORRELATION_RESULT broadcast)"
    - "src/dashboard/hooks/useCorrelations.ts (exposes a loaded flag set when the mount-load settles)"
    - "src/dashboard/App.tsx (corrInitRef auto-run gated on loaded + hasFreshAnalysis; header freshness badge with local timeAgo helper)"
    - "tests/unit/correlation-persistence.test.ts (NEW — write-policy + stamp + existence-predicate + corrupt-tolerant read coverage)"
    - "tests/e2e/fixtures.ts (MOCK_CORRELATIONS gains computedAt/model/inputCounts; CORRELATE_ALL invocation counter added to the mock runtime)"
    - "tests/e2e/dashboard.spec.ts (5 new Correlations Tab tests: no-auto-analyze, auto-run-when-empty, header computedAt+engine, legacy unknown-age, stale marker)"
  key_links:
    - "All six background terminal write sites ↔ persistCorrelationResult(storage, CONFIG.storage.correlations, stamped) — the single write-policy choke point"
    - "App.tsx auto-run gate ↔ hasFreshAnalysis(correlations) AND useCorrelations loaded flag AND snapshot-has-data guard"
    - "App.tsx header badge ↔ correlations.computedAt/engine/model read directly off the stored result (never runStats, which is null on cache-load)"
    - "e2e CORRELATE_ALL counter (globalThis.__trendcastCorrelateAllCalls) ↔ the auto-run gate (0 calls with fresh cache, >=1 with no stored result)"
  flagged_assumptions:
    - "EDGE PROBES for TRIG-01/TRIG-02/TRIG-04 are authored here (all were unresolved in the spec-less fallback report); TRIG-03's three edge probes are authored in 16-02-PLAN.md — together all 8 unresolved items are surfaced, none silently dropped."
    - "TRIG-01 adjacency (flagged assumption): when an incoming error result meets a stored non-error result they do NOT merge or collide — the stored non-error result is preserved and only the broadcast carries the error."
    - "TRIG-01 empty (flagged assumption): an empty, null, or corrupt stored correlations value is treated as absent (no analysis), never as an error and never thrown."
    - "TRIG-01 ordering (flagged assumption): a single-element matches array renders normally with no special-casing — covered by the existing e2e 'News → Social (1)' assertions."
    - "TRIG-02 unclassified (flagged assumption): the auto-run gate is loaded && snapshot-has-data && !hasFreshAnalysis(correlations), evaluated once per dashboard session via corrInitRef."
    - "TRIG-04 unclassified (flagged assumption): the header badge reads computedAt/engine/model directly off the stored result — runStats stays null on cache-load and must not be used."
  prohibitions:
    - "An error correlation result must never overwrite a stored non-error correlation result in CONFIG.storage.correlations."
    - "The dashboard must never auto-run correlation analysis on open when a stored non-error result exists (auto-analyze on every tab open is out of scope per REQUIREMENTS.md)."
    - "A persistence failure must never break the run's terminal path — the CORRELATION_RESULT broadcast always fires."
---

<objective>
Make correlation results durable with freshness metadata and a clobber-proof write policy, and make the dashboard cached-first: every terminal run path persists a stamped result, errors never destroy good cached results, opening the dashboard shows the cache instantly without auto-analyzing, and the header shows how fresh the results are.

Purpose: TRIG-01 (metadata + write policy on every terminal path), TRIG-02 (no auto-analyze on open; auto-run only when no stored non-error analysis exists), and TRIG-04 (visible computedAt + engine) — the persistence and display half of Phase 16. TRIG-03 (post-collection triggers) is planned separately in 16-02 because it depends on this plan's computedAt metadata and helper module.

Output: src/utils/correlation-persistence.ts with the write-policy helpers, all six background terminal write sites routed through it, the gated auto-run, the header freshness badge, unit + e2e coverage. No new npm packages.
</objective>

## Artifacts this phase produces

New symbols created by Phase 16 (for plan-review-convergence source grounding — these are NEW, not pre-existing):

- New file: src/utils/correlation-persistence.ts
- Functions: stampCorrelationResult, persistCorrelationResult, hasFreshAnalysis, readStoredAnalysis (this plan); shouldTriggerReanalysis (16-02)
- Exported types: CorrelationInputCounts, CorrelationStampMeta (this plan); CorrelationRunLiveness (16-02)
- Type fields added to CorrelationResult in src/types/index.ts: computedAt?: number, model?: string, inputCounts?: { markets: number; signals: number; news: number }
- Hook surface: loaded flag returned from useCorrelations (this plan); last-applied dedupe + storage.onChanged listeners (16-02)
- App.tsx: local timeAgo helper + correlations header freshness badge (this plan)
- Tests: tests/unit/correlation-persistence.test.ts (NEW, this plan); e2e counter globalThis.__trendcastCorrelateAllCalls in tests/e2e/fixtures.ts (this plan); 5 new dashboard e2e tests (this plan) + 2 more (16-02)

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-correlation-persistence-analysis-triggers/16-RESEARCH.md
@.planning/phases/16-correlation-persistence-analysis-triggers/16-PATTERNS.md
@src/utils/ml-run-state.ts
@src/utils/settings.ts
@src/dashboard/hooks/useSnapshot.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — one terminal path persists a stamped result; dashboard shows it instantly with freshness and no auto-run</name>
  <reversibility rating="reversible">Additive optional fields on CorrelationResult and a new helper behind the narrow SettingsStorage interface — undo is deleting one file and reverting call sites; no stored-data migration needed since all new fields are optional.</reversibility>
  <files>src/types/index.ts, src/utils/correlation-persistence.ts, tests/unit/correlation-persistence.test.ts, src/background/index.ts, src/dashboard/hooks/useCorrelations.ts, src/dashboard/App.tsx, tests/e2e/fixtures.ts, tests/e2e/dashboard.spec.ts</files>
  <read_first>
    - src/utils/ml-run-state.ts (the SettingsStorage-convention analog: module docstring with phase tag, readMlRunState corrupt-tolerant read pattern lines 27-45, write/clear helpers)
    - src/utils/settings.ts (SettingsStorage interface, lines 19-25)
    - src/types/index.ts (CorrelationResult lines 262-283; CorrelationRunStats lines 286-296 for the epoch-ms/model doc-comment conventions to copy)
    - src/config/index.ts (CONFIG.storage block lines 182-203 — correlations key is 'trendcast:correlations')
    - src/background/index.ts (runCorrelationAsync: input-count log ~1043-1048, success write ~1056, CORRELATION_RESULT broadcast ~1058-1068 — this task touches ONLY the success write; catch-path/recovery/precompute are Task 2)
    - src/dashboard/hooks/useCorrelations.ts (mount-load effect lines 109-124; return object near line 347)
    - src/dashboard/App.tsx (corrInitRef auto-run effect lines 199-215; correlations header controls row lines 468-476; useSnapshot destructure at line 141 for lastCollectionAt)
    - src/dashboard/components/CorrelationPanel.tsx (timeAgo helper lines 136-144 — copy verbatim)
    - tests/unit/ml-run-queue.test.ts (mockStorage in-memory SettingsStorage mock, lines 131-139)
    - tests/e2e/fixtures.ts (MOCK_CORRELATIONS lines 212-247; mockBrowserApiScript runtimeSendMessage + cannedResponses ~365-400)
    - tests/e2e/dashboard.spec.ts (Dashboard — Correlations Tab describe from line 532; openDashboard(page, overrides) pattern)
  </read_first>
  <action>
    Wire ONE path end-to-end — the success terminal path — from background persist through storage to the dashboard display and the gated auto-run. Production quality; this slice becomes the skeleton the remaining write sites expand into.

    1. src/types/index.ts — append three OPTIONAL fields to CorrelationResult, doc-comment style matching CorrelationRunStats: computedAt?: number (epoch ms when the result was persisted), model?: string (model ID used, empty/omitted for heuristic), inputCounts?: { markets: number; signals: number; news: number }. All optional so results stored before this phase remain valid (research Pitfall 4).

    2. Create src/utils/correlation-persistence.ts mirroring src/utils/ml-run-state.ts exactly: module docstring tagged Phase 16 (TRIG-01) that states the write-policy decision table (success overwrites anything unless a non-error with strictly newer computedAt exists; error overwrites only absent/corrupt/error; error NEVER displaces a non-error — broadcast still happens at the call site), import type SettingsStorage from './settings', import CorrelationResult and CorrelationEngine from '@/types'. Exports:
       - CorrelationInputCounts interface: markets: number; signals: number; news: number.
       - CorrelationStampMeta interface: engine?: CorrelationEngine; model?: string; inputCounts?: CorrelationInputCounts.
       - stampCorrelationResult(result: CorrelationResult, meta: CorrelationStampMeta): CorrelationResult — pure; returns a NEW object (never mutates the input) with computedAt: Date.now(); applies meta.model when provided; applies meta.inputCounts when provided; applies meta.engine only when result.engine is undefined.
       - readStoredAnalysis(storage: SettingsStorage, key: string): Promise<CorrelationResult | null> — corrupt-tolerant like readMlRunState: try/catch returns null; returns null when the stored value is not an object or its matches field is not an array. Corrupt data degrades to no analysis, never throws.
       - hasFreshAnalysis(stored: CorrelationResult | null | undefined): boolean — true iff stored is truthy AND stored.error is falsy. A persisted error result therefore counts as no analysis (success criterion 2, second clause — research Pitfall 3).
       - persistCorrelationResult(storage: SettingsStorage, key: string, result: CorrelationResult): Promise<boolean> — read-modify-write enforcing the policy: read existing via readStoredAnalysis; if the incoming result has no error, overwrite UNLESS the existing result is non-error AND has a numeric computedAt strictly greater than the incoming computedAt (keeps a genuinely newer result; stamping happens immediately before persist so computedAt reflects write order); if the incoming result has an error, overwrite only when existing is absent/corrupt or itself an error, otherwise skip the write and return false. Wrap the storage.set in try/catch — a storage failure returns false instead of throwing, so a persistence failure can never break the caller's terminal path.

    3. Write tests/unit/correlation-persistence.test.ts FIRST (watch the new imports fail, then implement step 2): header block comment listing verified behaviors, vitest imports, the mockStorage() in-memory SettingsStorage mock copied from tests/unit/ml-run-queue.test.ts lines 131-139, KEY = 'trendcast:correlations'. Cases: stamp adds a numeric computedAt plus model plus inputCounts and does not mutate its input; persist success over absent returns true and stores; success over stored error returns true; success over stored success with older computedAt returns true; success over stored success with strictly newer computedAt returns false and keeps the stored value; error over absent returns true; error over stored error returns true; error over stored non-error returns false AND the stored value is unchanged; hasFreshAnalysis null/undefined/false-case with error result/true for good result/true for legacy good result lacking computedAt; readStoredAnalysis returns null for absent, for a string, and for an object whose matches is not an array, and returns the object for valid data.

    4. src/background/index.ts success site only: in runCorrelationAsync, replace the direct correlations-key storage write (line ~1056) with stampCorrelationResult(result, { model, inputCounts: { markets: markets.length, signals: signals.length, news: news.length } }) followed by persistCorrelationResult(browser.storage.local, CONFIG.storage.correlations, stamped); broadcast the STAMPED result in the existing CORRELATION_RESULT sendMessage (unchanged otherwise). Do NOT touch the catch-path, recovery, or precompute writes in this task — Task 2 routes them.

    5. src/dashboard/hooks/useCorrelations.ts — add a loaded flag: const [loaded, setLoaded] = useState(false); in the mount-load effect chain a .finally(() => setLoaded(true)) after the existing .catch; add loaded to the returned object.

    6. src/dashboard/App.tsx auto-run gate (TRIG-02): destructure loaded as corrLoaded from useCorrelations; import hasFreshAnalysis from '@/utils/correlation-persistence'; restructure the corrInitRef effect to: keep the existing once-per-session ref and the snapshot/snapshot-has-data guards; add an early return when corrLoaded is false (before touching the ref); then set corrInitRef.current = true; then early-return when hasFreshAnalysis(correlations) is true; otherwise run the existing initModel derivation chain verbatim and call runCorrelation(settings.correlationEngine, initModel). Setting the ref before the freshness check guarantees the gate is evaluated exactly once per session (a mid-session manual failure cannot re-trigger an auto-run).

    7. src/dashboard/App.tsx header badge (TRIG-04): copy the timeAgo helper verbatim from src/dashboard/components/CorrelationPanel.tsx lines 136-144 into App.tsx as a module-private function. In the correlations header controls row (the flex div containing the engine select, line ~470), render a small text span before the engine selector when correlations is truthy AND corrLoading is false: when typeof correlations.computedAt === 'number' the text is '⏱ computed ' + timeAgo(correlations.computedAt) + ' · ' + (correlations.engine ?? 'unknown engine'), appending ' · ' + correlations.model when correlations.model is a non-empty string, and appending ' · ⚠ stale' when lastCollectionAt is a number AND correlations.computedAt < lastCollectionAt; when computedAt is absent (legacy result) the text is '⏱ computed: unknown age · ' + (correlations.engine ?? 'unknown engine'). Render via plain React text interpolation only (no HTML injection); use text-[10px] muted styling consistent with neighboring header text.

    8. tests/e2e/fixtures.ts — extend MOCK_CORRELATIONS with computedAt: Date.now(), model: '', inputCounts: { markets: 2, signals: 3, news: 2 } (matching MOCK_SNAPSHOT array lengths). In the mock runtime's runtimeSendMessage, increment a global counter when the message type is CORRELATE_ALL: set globalThis.__trendcastCorrelateAllCalls to (existing ?? 0) + 1 at the TOP of runtimeSendMessage — before the custom __messageHandlers loop AND before the canned-response lookup — because a custom handler that serves the message causes an early return, so any increment placed after the loop never runs for handler-served CORRELATE_ALL calls; specs use this counter to assert how many auto-runs fired.

    9. tests/e2e/dashboard.spec.ts — add two tests to the Dashboard — Correlations Tab describe, following its openDashboard → click Correlations → waitForTimeout rhythm: (a) 'shows cached results without auto-analyze': openDashboard(page) with defaults, open the Correlations tab, wait 1000ms, then evaluate the counter via page.evaluate(() => (globalThis as unknown as { __trendcastCorrelateAllCalls?: number }).__trendcastCorrelateAllCalls ?? 0) and expect 0, and expect main to contain the cached match text 'BTC to the moon'; (b) 'header shows computedAt and engine from cached result': expect main to contain text matching /computed \d+[smhd] ago/ and to contain text 'heuristic'.
  </action>
  <verify>
    <automated>bun run test -- --run tests/unit/correlation-persistence.test.ts && bun run typecheck && bun run test:e2e -- dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/correlation-persistence.ts exists and exports stampCorrelationResult, persistCorrelationResult, hasFreshAnalysis, readStoredAnalysis, CorrelationInputCounts, CorrelationStampMeta.
    - src/types/index.ts CorrelationResult declares optional computedAt, model, and inputCounts fields.
    - bun run test -- --run tests/unit/correlation-persistence.test.ts exits 0, including the case where an error result does not overwrite a stored non-error result (test asserts the stored value is unchanged and the helper returned false).
    - bun run typecheck exits 0.
    - The runCorrelationAsync success write in src/background/index.ts calls persistCorrelationResult with CONFIG.storage.correlations and a stamped result (grep finds at least one persistCorrelationResult call site).
    - bun run test:e2e -- dashboard.spec.ts passes, including: the no-auto-analyze test asserts the CORRELATE_ALL counter evaluates to 0 with fresh cached results; the header test asserts /computed \d+[smhd] ago/ and 'heuristic' render from the cached result.
  </acceptance_criteria>
  <done>
    The success terminal path persists a stamped result through the write-policy helper; the dashboard renders cached results instantly with a computedAt + engine badge and sends zero CORRELATE_ALL messages when a non-error result is stored; the write-policy and existence predicate are unit-tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route the remaining five terminal write sites through the write-policy (error/cancel/precompute/recovery)</name>
  <reversibility rating="reversible">The policy lives inside one helper; the call-site reroute is a mechanical swap reversible per site with no data migration.</reversibility>
  <files>src/background/index.ts</files>
  <read_first>
    - src/background/index.ts (SW-death recovery block lines 337-368; runCorrelationAsync catch-path error construction + write lines 1085-1105; runCorrelationPrecompute lines 1225-1265; the fire-and-forget precompute call lines 996-1001; the non-fatal marker-write try/catch pattern lines 1030-1035)
    - src/utils/correlation-persistence.ts (created in Task 1 — stampCorrelationResult, persistCorrelationResult signatures)
    - src/utils/ml-run-state.ts (MlRunState shape — marker.model available on the recovery path)
  </read_first>
  <action>
    Route every remaining terminal write through the Task 1 helper so TRIG-01's 'every terminal path' clause holds. Keep every CORRELATION_RESULT broadcast — the write-policy must never suppress the broadcast the active run's UI depends on (research Q5).

    1. runCorrelationAsync catch-path (lines ~1089-1100): keep the errorResult construction; stamp it via stampCorrelationResult(errorResult, { engine, model }) — model is the function parameter; inputCounts are NOT available here (the data loads inside the try), so omit them — then replace the direct correlations-key write with persistCorrelationResult(browser.storage.local, CONFIG.storage.correlations, stamped). The policy now prevents a cancel or ML error from clobbering a fresh good cached result while the broadcast still settles the waiting tab.

    2. SW-death recovery (lines ~340-362): stamp interruptedResult via stampCorrelationResult(interruptedResult, { model: marker.model }) and replace the direct write with persistCorrelationResult(browser.storage.local, CONFIG.storage.correlations, stamped) — the helper's error-vs-good policy fixes the clobber-a-fresh-result bug (research Q1 site 6). Keep the existing clearMlRunState call and the CORRELATION_RESULT broadcast.

    3. runCorrelationPrecompute (def ~1227, write ~1241): wrap the runCorrelationWithEngine call in a local try/catch — on throw, build a terminal error result (empty matches/newsMatches/newsSocialMatches/newsNewsMatches arrays, engine, requestId 'precompute-' + Date.now(), error from err instanceof Error ? err.message : String(err)) so a cancelled or thrown precompute finally reaches a terminal write (research Q1 site 5 — today it writes nothing and the rejection is swallowed by the outer .catch at line ~1000). Then stamp with { model, inputCounts: { markets: markets.length, signals: signals.length, news: news.length } } (all three are function parameters, available even on the catch path), persist via the helper, and add a CORRELATION_RESULT broadcast of the stamped result with .catch(() => {}) — per the research synthesis decision that precompute joins the same persist+broadcast path so an open dashboard learns about post-collection results. Keep the existing runAlertSweep, rebuildMarketNewsView, and success/error logging.

    4. Non-fatal discipline: persistCorrelationResult already internalizes its storage errors and returns false; at each call site, when it returns false because the policy kept an existing non-error result, log a single console.warn with the '[TrendCast]' prefix noting the existing non-error result was kept. No persistence failure may throw into the terminal path.
  </action>
  <verify>
    <automated>bun run test -- --run tests/unit/correlation-persistence.test.ts && bun run test && bun run typecheck && bun run lint && grep -c "persistCorrelationResult" src/background/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "persistCorrelationResult" src/background/index.ts reports at least 4 call sites (success, catch-path, recovery, precompute).
    - runCorrelationPrecompute contains a local try/catch around runCorrelationWithEngine, so a thrown or cancelled precompute produces a terminal error-result write instead of writing nothing.
    - runCorrelationPrecompute broadcasts CORRELATION_RESULT with the stamped payload.
    - The SW-death recovery write goes through the helper, so an interrupted error result no longer displaces a stored non-error result.
    - bun run typecheck and bun run lint exit 0; bun run test (full unit suite, 416+ baseline) stays green.
  </acceptance_criteria>
  <done>
    All six terminal write sites (success, catch-path error, ML-error return via the success path, precompute success, precompute throw/cancel, SW-death recovery) persist through the write-policy helper with stamped metadata, and every path still broadcasts CORRELATION_RESULT.
  </done>
</task>

<task type="auto">
  <name>Task 3: Complete e2e coverage — auto-run-when-empty, legacy unknown-age badge, stale marker</name>
  <files>tests/e2e/dashboard.spec.ts</files>
  <read_first>
    - tests/e2e/dashboard.spec.ts (Dashboard — Correlations Tab describe from line 532; the Task 1 tests for rhythm and the counter-evaluate pattern)
    - tests/e2e/fixtures.ts (MOCK_CORRELATIONS now carrying computedAt/model/inputCounts from Task 1; 'trendcast:last-collection' seeded at Date.now() - 1_800_000; openDashboard(page, overrides) signature)
  </read_first>
  <action>
    Add three tests to the Dashboard — Correlations Tab describe, matching the existing openDashboard → click Correlations → waitForTimeout rhythm:

    1. 'auto-runs analysis when no stored result exists': openDashboard(page, { 'trendcast:correlations': null }) — the mock's get returns the null value, the hook skips it, and the gate sees no analysis. Open the Correlations tab, wait 2000ms, then evaluate (globalThis as unknown as { __trendcastCorrelateAllCalls?: number }).__trendcastCorrelateAllCalls ?? 0 and expect it to be at least 1. This proves the gate opens both ways (paired with Task 1's zero-call test).

    2. 'header shows unknown age for legacy results without computedAt': build a legacy fixture by destructuring computedAt out of MOCK_CORRELATIONS (const { computedAt: _omitted, ...legacyCorrelations } = MOCK_CORRELATIONS), openDashboard(page, { 'trendcast:correlations': legacyCorrelations }), open the Correlations tab, and expect main to contain text matching /unknown age/i. Legacy cached results must display, never crash (research Pitfall 4).

    3. 'header marks stale results after a newer collection': openDashboard(page, { 'trendcast:correlations': { ...MOCK_CORRELATIONS, computedAt: Date.now() - 7_200_000 } }) — computedAt 2h ago vs the seeded last-collection 30min ago. Open the Correlations tab and expect main to contain text matching /stale/i.

    Finish with the full regression gate: bun run test, bun run typecheck, bun run lint.
  </action>
  <verify>
    <automated>bun run test:e2e -- dashboard.spec.ts && bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - The auto-run-when-empty test observes at least 1 CORRELATE_ALL invocation after opening the dashboard with no stored correlations.
    - The legacy test observes 'unknown age' in the Correlations header for a stored result lacking computedAt.
    - The stale test observes the stale marker when computedAt is older than the last collection time.
    - All five new Correlations Tab e2e tests (2 from Task 1 + 3 here) pass; the full unit suite, typecheck, and lint exit 0.
  </acceptance_criteria>
  <done>
    The auto-run gate, the legacy-result display path, and the stale marker are each proven by an automated e2e test, and the full suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser.storage.local → background/dashboard readers | Stored correlation results (written by any past run, possibly from an older app version or corrupted) cross into engine-agnostic readers and the React UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering (V5 input validation) | readStoredAnalysis / hasFreshAnalysis / mount-load of the correlations key | low | mitigate | Corrupt-tolerant read pattern copied from readMlRunState: non-object stored values and objects whose matches is not an array degrade to null ('no analysis'); hasFreshAnalysis treats error results as no analysis; nothing throws into the UI |
| T-16-02 | Tampering | App.tsx header badge rendering stored engine/model strings | low | mitigate | Badge renders stored strings via React text interpolation (auto-escaped, no dangerouslySetInnerHTML); computedAt is rendered only through the timeAgo Date-math formatter, never raw |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages — pure code change across existing files plus one new util module; no install tasks, so the package-legitimacy gate has nothing to gate |
</threat_model>

<verification>
- bun run test — full unit suite green (416+ baseline plus the new correlation-persistence tests).
- bun run typecheck && bun run lint — both exit 0 (lint is max-warnings 0).
- bun run test:e2e -- dashboard.spec.ts — all existing Correlations Tab tests plus the five new ones pass.
- bun run build:debug:firefox — production build succeeds before phase verification.
- Manual (end-of-phase, per 16-VALIDATION.md): reload extension with existing data → cached results render instantly with no auto-run; force an ML error offline → the cached good result survives and the error shows without clobbering; restart browser → results still present.
</verification>

<success_criteria>
- Every terminal run path writes CONFIG.storage.correlations with computedAt + input metadata (ROADMAP success criterion 1).
- An error result never clobbers a fresh good cached result, and a persisted error result never suppresses future auto-analysis (ROADMAP success criterion 2).
- Opening the dashboard shows cached results instantly with no auto-analyze; analysis auto-runs only when no stored non-error analysis exists (ROADMAP success criterion 3, first half).
- The Correlations header shows computedAt + engine so live vs cached is distinguishable (ROADMAP success criterion 4, badge half).
</success_criteria>

<output>
Create `.planning/phases/16-correlation-persistence-analysis-triggers/16-01-SUMMARY.md` when done
</output>