---
phase: 06-watchlist-export
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/utils/export.ts
  - tests/unit/export.test.ts
autonomous: true
requirements:
  - DASH-02
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - The `# News` CSV section gains a trailing `category` column; all existing columns stay in the same order/position (D-01, D-02)
    - The JSON export includes `category` on each news object (D-02)
    - Existing CSV sections and column order are unchanged — the change is append-only (D-01)
    - No separate `# Market-Driven News` section is added (D-03)
    - News items without a `category` export an empty string (no `undefined` in the CSV) (Pitfall 8)
  artifacts:
    - `category` trailing column in the `# News` section of `exportToCsv` in src/utils/export.ts
    - `category` field on news objects in `exportToJson` in src/utils/export.ts
    - tests/unit/export.test.ts
  key_links:
    - The `# News` header row includes `category` as the last column (D-02)
    - `exportToJson` news objects carry `category` (D-02)
    - `NewsItem.category` is optional, so export defaults missing values to `''` (Pitfall 8)
---

<objective>
Add `category` as a trailing column on the existing `# News` CSV section and as a field on news objects in the JSON export, keeping the export backward-compatible (append-only).

Purpose: DASH-02 requires export coverage for new sources (market-driven categories). D-01..D-03 lock the backward-compatible format: existing sections/columns unchanged, `category` appended as a trailing column on the existing `# News` section (and a field on JSON news objects), and no separate market-driven section. This plan is the export layer over the `NewsItem.category` field already persisted in Phase 5.

Output: `category` trailing column in `exportToCsv`'s `# News` section, `category` on news objects in `exportToJson`, and `tests/unit/export.test.ts`.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/06-watchlist-export/06-CONTEXT.md
@.planning/phases/06-watchlist-export/06-RESEARCH.md

# Existing code this plan builds on
@src/utils/export.ts
@src/types/index.ts
@tests/unit/fixtures.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add category trailing column to News CSV + JSON export + unit tests</name>
  <files>src/utils/export.ts, tests/unit/export.test.ts</files>
  <behavior>
    - Test 1 (CSV trailing column, D-02): the `# News` section header row is `id,source,headline,summary,url,publishedAt,keywords,category` — `category` is the LAST column, all prior columns unchanged
    - Test 2 (CSV value, D-02): a news item with `category: 'finance'` exports `finance` in the last column of its row
    - Test 3 (CSV missing category, Pitfall 8): a news item with no `category` exports an empty string in the last column (no `undefined`)
    - Test 4 (backward-compat, D-01): the `# Markets`, `# Social Signals`, `# Correlations (Social → Market)`, and `# Correlations (News → Market)` section headers and column orders are unchanged from before this change
    - Test 5 (JSON, D-02): `exportToJson` news objects include a `category` field
    - Test 6 (no new section, D-03): the CSV does NOT contain a `# Market-Driven News` section
  </behavior>
  <action>
    Extend the export to cover the `category` field backward-compatibly (D-01, D-02, D-03):

    1. `src/utils/export.ts` — in `exportToCsv`, the `# News` section (line ~90):
       - Add `category: n.category ?? ''` to the row object (default missing to empty string so no `undefined` appears in the CSV — Pitfall 8).
       - Append `'category'` to the headers array as the LAST element: `['id', 'source', 'headline', 'summary', 'url', 'publishedAt', 'keywords', 'category']`.
       Do NOT reorder or change any existing column. Do NOT add a `# Market-Driven News` section (D-03) — the category surfaces via the trailing column on news rows.

    2. `src/utils/export.ts` — in `exportToJson`, the news objects already carry `category` because `ExportData.news` is `NewsItem[]` and `NewsItem` has the optional `category` field (added in Phase 5). No change needed to the JSON structure itself — verify the field flows through. If `exportToJson` maps news objects, ensure `category` is included (it is, since it spreads the `NewsItem`).
    Create `tests/unit/export.test.ts` covering the behaviors above. Use `bun run test -- tests/unit/export.test.ts` as the automated gate.
  </action>
  <verify>
    <automated>bun run test -- tests/unit/export.test.ts</automated>
  </verify>
  <done>
    The `# News` CSV section has `category` as its last column; news rows export the category (or empty string when missing). The JSON export includes `category` on news objects. No `# Market-Driven News` section is added. All export unit tests pass.
  </done>
  <reversibility rating="costly">Changes the `# News` section header (adds a trailing column) — header-aware readers see a new column, though positional readers are unaffected (D-02).</reversibility>
</task>

<task type="task">
  <name>Task 2: Verify export backward-compatibility end-to-end</name>
  <files>src/utils/export.ts, tests/unit/export.test.ts</files>
  <action>
    Add a regression test that locks the backward-compatibility contract (D-01) so future export changes can't silently break existing consumers:

    1. In `tests/unit/export.test.ts`, add a test that builds a full `ExportData` fixture (reuse `mockContract`, `mockSignal`, `newsItem()` from `tests/unit/fixtures.ts`) and asserts:
       - The `# Markets` section header is exactly `id,platform,question,volume24h,endDate,outcomes,lastUpdated`.
       - The `# Social Signals` section header is exactly `id,platform,text,author,likes,shares,comments,views,sentiment,virality,timestamp,keywords`.
       - The `# Correlations (Social → Market)` header is exactly `signalId,signalPlatform,signalText,contractId,contractPlatform,contractQuestion,confidence,matchedKeywords,correlatedAt`.
       - The `# Correlations (News → Market)` header is exactly `newsId,newsSource,newsHeadline,contractId,contractPlatform,contractQuestion,confidence,matchedKeywords,correlatedAt`.
       - The `# News` header is exactly `id,source,headline,summary,url,publishedAt,keywords,category` (the ONLY section with a new trailing column).
       This test is the guard against future column reordering/renaming (Pitfall 8 — backward-compatible export).
    Do NOT change any existing export section or column order — this task only adds the regression test that documents the contract.
  </action>
  <verify>
    <automated>bun run test -- tests/unit/export.test.ts</automated>
  </verify>
  <done>
    A regression test asserts the exact header rows for all 5 CSV sections, locking the backward-compatible format (only `# News` gains the trailing `category` column). All export tests pass.
  </done>
  <reversibility rating="reversible">Test-only — no production code change; safe to adjust.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored news → exportToCsv/exportToJson | untrusted stored news data enters the export serializer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | export CSV injection | low | mitigate | `csvEscape` already quotes commas/quotes/newlines; `category` is a controlled enum value, not free text — no formula-injection surface |
| T-06-06 | DoS | export size | low | mitigate | Export is a one-shot user-triggered download over existing bounded data; no unbounded growth introduced |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase — all work reuses the existing stack. No package legitimacy gate required. |
</threat_model>

<verification>
- `bun run test -- tests/unit/export.test.ts` passes (trailing column, value, missing-category empty string, backward-compat headers, JSON category, no new section).
- `grep -n "'category'" src/utils/export.ts` shows `category` in the `# News` headers array and the row object.
- `grep -n "Market-Driven News" src/utils/export.ts` returns nothing (no new section — D-03).
</verification>

<success_criteria>
- The `# News` CSV section has `category` as a trailing column; all existing columns/sections are unchanged (append-only).
- The JSON export includes `category` on news objects.
- No separate `# Market-Driven News` section is added (D-03).
- News items without a `category` export an empty string (no `undefined`).
</success_criteria>

<output>
Create `.planning/phases/06-watchlist-export/06-03-SUMMARY.md` when done
</output>
