---
phase: 03-correlation-speedup
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/services/engine/correlation.ts
  - tests/unit/correlation-equivalence.test.ts
autonomous: true
requirements:
  - PERF-02
estimate:
  tokens: 60000
  raw_tokens: 35000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "The heuristic path (`correlate`, `correlateNews`, `correlateNewsSocial`) pre-filters candidate contracts via `InvertedIndex.candidates` instead of the O(n×m) nested loop."
    - "The indexed heuristic output is identical to the naive loop — same matches, same confidence, same order (golden-test equivalence, D-02)."
    - "The tiny-input fallback keeps the naive loop when the index is not built, so results are unchanged for tiny inputs (D-03)."
    - "The heuristic path indexes both `contract.keywords` and entity-derived keywords so entity-only matches are preserved (superset invariant)."
  artifacts:
    - src/services/engine/correlation.ts
    - tests/unit/correlation-equivalence.test.ts
  key_links:
    - correlatePair/correlateNewsPair/correlateNewsSocialPair bodies remain byte-for-byte identical (equivalence oracle)
    - InvertedIndex.candidates(signal.keywords) feeds the inner loop in all three heuristic functions
---

<objective>
Convert the heuristic correlation path (`correlate`, `correlateNews`, `correlateNewsSocial` in `src/services/engine/correlation.ts`) from the O(n×m) nested loop to candidate-filtered via the shared `InvertedIndex`, and prove equivalence with the naive loop via a dedicated equivalence test file.

Purpose: The heuristic path is the default engine and the biggest O(n×m) beneficiary. Its naive loop is also the reference oracle for the whole phase — so the conversion must preserve it byte-for-byte as the equivalence baseline.
Output: `src/services/engine/correlation.ts` (candidate-filtered), `tests/unit/correlation-equivalence.test.ts` (heuristic equivalence + edge cases).
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-correlation-speedup/03-RESEARCH.md
@.planning/phases/03-correlation-speedup/03-PATTERNS.md
@.planning/phases/03-correlation-speedup/03-VALIDATION.md
@src/services/engine/correlation.ts
@src/services/engine/index.ts
@src/utils/keywords.ts
@src/utils/entities.ts
@src/types/index.ts
@tests/unit/correlation.test.ts
@tests/unit/fixtures.ts
</context>

## Artifacts this phase produces

- `src/services/engine/correlation.ts` — modified `correlate`/`correlateNews`/`correlateNewsSocial` to consume `InvertedIndex.candidates`
- `tests/unit/correlation-equivalence.test.ts` — heuristic equivalence + edge cases

<tasks>

<task type="tracer">
  <name>Task 1: Convert the heuristic `correlate` path to candidate-filtered + equivalence test</name>
  <files>src/services/engine/correlation.ts, tests/unit/correlation-equivalence.test.ts</files>
  <read_first>
    - src/services/engine/correlation.ts (the `correlate` function lines 112-137 and `correlatePair` lines 139-200 — the naive loop to convert and the oracle to preserve)
    - src/services/engine/index.ts (the `InvertedIndex` and `getIncrementalIndex` just created)
    - src/utils/entities.ts (`extractEntityKeywords` at line 360 — entity tokenizer for the superset)
    - tests/unit/fixtures.ts (shared fixtures)
  </read_first>
  <action>
    In `src/services/engine/correlation.ts`, import `getIncrementalIndex` from `./index`. In `correlate(signals, contracts)`:
    - Build the index once per call: `const index = getIncrementalIndex(contracts, { includeEntityKeywords: true })`. The `includeEntityKeywords: true` flag makes the index a superset of BOTH keyword and entity matches (Open Question 3 in RESEARCH.md) — this preserves entity-only matches that the naive loop would produce via `cachedEntitySimilarity`.
    - Add the tiny-input fallback: if `contracts.length < InvertedIndex.TINY_INPUT_THRESHOLD`, keep the existing naive nested loop unchanged (D-03).
    - Otherwise, replace the inner `for (const contract of contracts)` loop with `for (const i of index.candidates(signal.keywords))` and index into `contracts[i]`. The `correlatePair(signal, contract, cache)` body must remain byte-for-byte identical — do not touch `correlatePair`, `cachedEntitySimilarity`, `EntityCache`, or any constant (`MIN_CONFIDENCE`, `MIN_CONFIDENCE_ENTITY_MATCH`, `CASHTAG_BOOST`, `ENTITY_WEIGHT`, `KEYWORD_WEIGHT`).
    - Preserve the `console.debug` logging and the final `matches.sort((a, b) => b.confidence - a.confidence)` exactly.

    Create `tests/unit/correlation-equivalence.test.ts` (per D-04). It must run BOTH the naive loop (reference oracle) and the indexed path over the same shared fixtures and assert identical results — same matches, same confidence, same order. Structure:
    - A local `naiveCorrelate(signals, contracts)` that replicates the original nested loop (copy the loop shape from the pre-change `correlate`), used as the oracle.
    - Import the production `correlate` (indexed path) from `@/services/engine/correlation`.
    - For each fixture set, assert `indexed` deep-equals `naive` (compare `contract.id`, `signal.id`, `confidence`, `matchedKeywords`, and order).
    - Include the hand-verified golden fixtures from `tests/unit/fixtures.ts` (D-02) asserting exact expected matches.
    - Include edge cases per D-03: empty keyword arrays, single contract, single signal, duplicate keywords, cashtag/hashtag-only texts, and the tiny-input fallback path (fewer than `TINY_INPUT_THRESHOLD` contracts → naive path used).
  </action>
  <verify>
    <automated>bun run test -- tests/unit/correlation-equivalence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- tests/unit/correlation-equivalence.test.ts` passes.
    - `correlate` uses `getIncrementalIndex(contracts, { includeEntityKeywords: true })` and `index.candidates(signal.keywords)`; the tiny-input fallback keeps the naive loop for `contracts.length < TINY_INPUT_THRESHOLD`.
    - `correlatePair` and all thresholds are unchanged (byte-for-byte).
    - The equivalence test asserts indexed output deep-equals naive output (same matches, confidence, order) over shared + golden fixtures.
  </acceptance_criteria>
  <done>
    - `bun run test -- tests/unit/correlation-equivalence.test.ts` passes.
    - `correlate` uses `getIncrementalIndex(contracts, { includeEntityKeywords: true })` and `index.candidates(signal.keywords)`; the tiny-input fallback keeps the naive loop for `contracts.length < TINY_INPUT_THRESHOLD`.
    - `correlatePair` and all thresholds are unchanged (byte-for-byte).
    - The equivalence test asserts indexed output deep-equals naive output (same matches, confidence, order) over shared + golden fixtures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert `correlateNews` and `correlateNewsSocial` to candidate-filtered + extend equivalence</name>
  <files>src/services/engine/correlation.ts, tests/unit/correlation-equivalence.test.ts</files>
  <read_first>
    - src/services/engine/correlation.ts (the `correlateNews` lines 202-260 and `correlateNewsSocial` lines 263-330 — the two remaining nested loops)
    - src/services/engine/index.ts (the `InvertedIndex` and `getIncrementalIndex`)
    - tests/unit/correlation-equivalence.test.ts (the equivalence test just created)
    - tests/unit/fixtures.ts (the `newsItem` factory)
  </read_first>
  <action>
    In `src/services/engine/correlation.ts`, apply the same candidate-filtered conversion to `correlateNews(news, contracts)` and `correlateNewsSocial(news, signals)`:
    - `correlateNews`: build `getIncrementalIndex(contracts, { includeEntityKeywords: true })`; tiny-input fallback when `contracts.length < TINY_INPUT_THRESHOLD`; inner loop over `index.candidates(item.keywords)` indexing `contracts[i]`. Keep `correlateNewsPair` byte-for-byte.
    - `correlateNewsSocial`: this correlates news against signals (not contracts). Build `getIncrementalIndex(signals, { includeEntityKeywords: true })` (the index is over the `signals` array, since the inner loop iterates signals); tiny-input fallback when `signals.length < TINY_INPUT_THRESHOLD`; inner loop over `index.candidates(item.keywords)` indexing `signals[i]`. Keep `correlateNewsSocialPair` byte-for-byte.
    - Preserve all `console.debug` logging and the final sorts.

    Extend `tests/unit/correlation-equivalence.test.ts` with `naiveCorrelateNews` and `naiveCorrelateNewsSocial` oracles (replicating the pre-change loops) and assert the indexed `correlateNews`/`correlateNewsSocial` deep-equal them over the shared fixtures (news + contracts, news + signals), including the golden fixtures and the D-03 edge cases (empty keywords, single contract/signal, dup keywords, cashtag/hashtag-only, tiny-input fallback).
  </action>
  <verify>
    <automated>bun run test -- tests/unit/correlation-equivalence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- tests/unit/correlation-equivalence.test.ts` passes.
    - `correlateNews` and `correlateNewsSocial` use `getIncrementalIndex` + `index.candidates` with the tiny-input fallback; their pair functions and thresholds are unchanged.
    - The equivalence test covers all three heuristic functions (`correlate`, `correlateNews`, `correlateNewsSocial`) against their naive oracles.
  </acceptance_criteria>
  <done>
    - `bun run test -- tests/unit/correlation-equivalence.test.ts` passes.
    - `correlateNews` and `correlateNewsSocial` use `getIncrementalIndex` + `index.candidates` with the tiny-input fallback; their pair functions and thresholds are unchanged.
    - The equivalence test covers all three heuristic functions (`correlate`, `correlateNews`, `correlateNewsSocial`) against their naive oracles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collector → heuristic engine | `signal.keywords`/`contract.keywords`/`news.keywords` arrays cross into the index and matcher; already normalized by `extractKeywords` (single source) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-03 | Tampering | `correlate`/`correlateNews`/`correlateNewsSocial` | low | mitigate | Build the index only from `contract.keywords`/`signal.keywords` (already normalized); never re-tokenize with a different function (single tokenization source, Pitfall 1) |
| T-3-04 | DoS | candidate pre-filter | low | mitigate | `InvertedIndex.candidates` dedups via `Set` and returns order-preserving indices; index size bounded by the build cap (T-3-02) |
| T-3-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new runtime dependencies; no package installs, gate not applicable |
</threat_model>

<verification>
- `bun run test -- tests/unit/correlation-equivalence.test.ts` green (heuristic equivalence)
- `bun run test` green (all unit tests, no regressions)
</verification>

<success_criteria>
- All three heuristic functions (`correlate`, `correlateNews`, `correlateNewsSocial`) are candidate-filtered via the shared index with the tiny-input fallback.
- The indexed heuristic output is provably identical to the naive loop (same matches, confidence, order) across shared + golden fixtures and all D-03 edge cases.
- The `correlatePair`/`correlateNewsPair`/`correlateNewsSocialPair` bodies and thresholds are unchanged (equivalence oracle preserved).
</success_criteria>

<output>
Create `.planning/phases/03-correlation-speedup/03-02-SUMMARY.md` when done
</output>
