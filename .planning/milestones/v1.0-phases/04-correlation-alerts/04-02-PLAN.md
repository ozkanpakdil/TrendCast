---
phase: 04-correlation-alerts
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/background/index.ts
  - src/background/alerts.ts
  - src/manifest.config.ts
  - src/config/index.ts
  - tests/unit/alerts.test.ts
autonomous: true
requirements: [ALERT-01]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "evaluateAlerts() is called after runCorrelationPrecompute() and runCorrelationAsync() complete (ALERT-01)"
    - "An alert-sweep alarm re-checks the last stored CorrelationResult so alerts survive the ephemeral MV3 worker"
    - "chrome.notifications.create() fires with a packaged iconUrl and only when getPermissionLevel() is 'granted' (D-07)"
    - "On permission 'denied', the toolbar badge shows the alert count via chrome.action.setBadgeText (D-07, D-09)"
    - "The badge auto-clears on a timer regardless of whether the Alerts tab was opened (D-09)"
    - "A notification-click handler opens the dashboard"
    - "The 'notifications' permission is added to the manifest"
  artifacts:
    - src/background/index.ts (setupAlarms alert-sweep, evaluateAlerts hook points, notification-click handler, ALERTS_UPDATED broadcast, CLEAR_ALERTS handler)
    - src/background/alerts.ts (dispatchAlerts, updateBadge, clearAlerts)
    - src/manifest.config.ts (permissions)
    - src/config/index.ts (CONFIG.alerts.alarmName)
  key_links:
    - "evaluateAlerts() → chrome.notifications.create() / chrome.action.setBadgeText"
    - "chrome.alarms alert-sweep → re-evaluate last stored CorrelationResult"
  prohibitions:
    - "Do NOT use timers for alerting — use chrome.alarms + persisted alertState (MV3 ephemeral worker)"
    - "Do NOT pass a remote iconUrl — always browser.runtime.getURL('icons/icon-128.png')"
    - "Do NOT create a notification without checking getPermissionLevel() first (D-07)"
---

<objective>
Wire the alert engine into the background orchestrator: call `evaluateAlerts()` after both correlation-completion hook points, register an alert-sweep `chrome.alarms` alarm so alerts survive worker restarts, dispatch `chrome.notifications` (with packaged `iconUrl` + `getPermissionLevel()` check), fall back to the toolbar badge on permission denial, add a notification-click handler, and add the `notifications` manifest permission.

Purpose: Make alerts actually fire in a real MV3 extension context, surviving the ephemeral service worker, with the permission-denied fallback (D-07) and time-based badge auto-clear (D-09).
Output: Background integration + manifest permission + alert dispatch/badge/clear functions.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-correlation-alerts/04-CONTEXT.md
@.planning/phases/04-correlation-alerts/04-RESEARCH.md
@.planning/phases/04-correlation-alerts/04-PATTERNS.md
@src/background/index.ts
@src/background/alerts.ts
@src/manifest.config.ts
@src/config/index.ts
@src/messaging/browser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notifications permission + alert-sweep alarm + evaluateAlerts hook points</name>
  <files>src/manifest.config.ts, src/config/index.ts, src/background/index.ts, src/background/alerts.ts</files>
  <read_first>src/manifest.config.ts (permissions :186-191), src/config/index.ts (CONFIG.collection :186, CONFIG.alerts), src/background/index.ts (setupAlarms :236, runCorrelationPrecompute :693, runCorrelationAsync :548, getSettings :761)</read_first>
  <action>
    In `src/manifest.config.ts`, add `'notifications'` to the `permissions` array (currently `['storage', 'alarms', 'tabs', 'scripting']`).

    In `src/config/index.ts`, ensure `CONFIG.alerts.alarmName` (`'trendcast-alert-sweep'`) and `CONFIG.alerts.sweepIntervalMinutes` (10) exist.

    In `src/background/index.ts`:
    - In `setupAlarms()` (:236), add `browser.alarms.create(CONFIG.alerts.alarmName, { periodInMinutes: CONFIG.alerts.sweepIntervalMinutes })` and a branch in the `onAlarm` listener for `alarm.name === CONFIG.alerts.alarmName` that re-reads the last stored `CorrelationResult` from `CONFIG.storage.correlations`, loads the watchlist from `CONFIG.storage.watchlist`, calls `getSettings()`, and invokes `evaluateAlerts(result, watchlist, settings)` so alerts fire even if the worker was killed mid-correlation.
    - After `runCorrelationPrecompute()` stores its result (:693, the `await browser.storage.local.set({ [CONFIG.storage.correlations]: result })` line), call `evaluateAlerts(result, watchlist, settings)`.
    - After `runCorrelationAsync()` stores + broadcasts its result (:548, after the `CORRELATION_RESULT` broadcast), call `evaluateAlerts(result, watchlist, settings)`.
    - Import `evaluateAlerts` from `@/background/alerts`.
  </action>
  <verify>
    <automated>bun run test tests/unit/alerts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `notifications` permission present; alert-sweep alarm registered; `evaluateAlerts()` called after both correlation-completion hook points; unit tests green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Notification dispatch + badge fallback + click handler + CLEAR_ALERTS</name>
  <files>src/background/alerts.ts, src/background/index.ts</files>
  <read_first>src/background/alerts.ts (evaluateAlerts), src/background/index.ts (setupMessageHandlers, broadcast pattern :585-592), src/messaging/browser.ts</read_first>
  <action>
    In `src/background/alerts.ts`, add:
    - `dispatchAlerts(records: AlertRecord[]): Promise<void>` — for each record, check `browser.notifications.getPermissionLevel()`; if `'granted'`, call `browser.notifications.create('trendcast-alert-' + record.contractId + '-' + record.alertedAt, { type: 'basic', iconUrl: browser.runtime.getURL('icons/icon-128.png'), title: record.direction + ' — ' + record.question, message: record.topSignalText ?? record.topNewsHeadline ?? '' })`; if `'denied'`, fall back to `updateBadge()` (D-07).
    - `updateBadge(): Promise<void>` — read `alertHistory`, count records within the last N hours (e.g. `CONFIG.alerts.badgeWindowHours`), call `browser.action.setBadgeText({ text: count > 0 ? String(count) : '' })`; time-based auto-clear (D-09).
    - `clearAlerts(): Promise<void>` — reset `alertHistory` to `[]`, clear `alertState`, clear the badge via `browser.action.setBadgeText({ text: '' })`, and broadcast `ALERTS_UPDATED` with the empty list (D-10).
    - `broadcastAlerts(records: AlertRecord[]): Promise<void>` — `browser.runtime.sendMessage({ type: 'ALERTS_UPDATED', payload: { alerts: records } })` with a `.catch()` (mirror the `CORRELATION_RESULT` broadcast pattern).

    In `src/background/index.ts`:
    - Add `browser.notifications.onClicked.addListener(...)` that opens the dashboard via `browser.tabs.create({ url: browser.runtime.getURL('dashboard/index.html') })` and clears the notification id.
    - In `setupMessageHandlers()`, add an `onMessage('CLEAR_ALERTS', ...)` handler that calls `clearAlerts()`.
    - After each `evaluateAlerts()` call, call `dispatchAlerts(newRecords)` and `broadcastAlerts(updatedHistory)`.
  </action>
  <verify>
    <automated>bun run test tests/unit/alerts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Notifications fire with packaged `iconUrl` only when permission is granted; badge fallback + time-based auto-clear on denial; click handler opens dashboard; `CLEAR_ALERTS` resets history + badge; unit tests green.
  </acceptance_criteria>
  <reversibility rating="reversible">UI/notification behavior is local; no external contract.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| engine → notifications | `chrome.notifications.create()` — requires permission; untrusted if permission abused |
| engine → badge | `chrome.action.setBadgeText` — badge state correctness |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Spoofing | chrome.notifications | medium | mitigate | Always pass packaged `iconUrl` via `browser.runtime.getURL('icons/icon-128.png')`; never a remote URL (blocked in MV3); check `getPermissionLevel()` before create (D-07) |
| T-4-03 | DoS | chrome.notifications | medium | mitigate | Global + per-market cooldown (CONFIG.alerts) throttle notification volume; dedup by stable key |
| T-4-04 | Tampering | chrome.action.setBadgeText | low | mitigate | Badge count derived from capped `alertHistory`; time-based auto-clear (D-09) prevents stale counts |
| T-4-05 | DoS | chrome.alarms alert-sweep | low | accept | Sweep re-evaluates last stored result; bounded by watchlist + cooldowns |
| T-4-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new runtime dependencies this phase; no package installs |
</threat_model>

<verification>
- `bun run test` (full unit suite) green
- Manual: load built extension in Chrome AND Firefox; verify notification appears for a watchlisted market and falls back to badge when permission denied (see 04-VALIDATION.md)
</verification>

<success_criteria>
- Alerts fire via `chrome.notifications` with packaged icon, surviving worker restarts via the alert-sweep alarm
- Permission-denied fallback to toolbar badge with time-based auto-clear
- Notification-click opens dashboard; `CLEAR_ALERTS` resets history + badge
- `notifications` permission added
</success_criteria>

<output>
Create `.planning/phases/04-correlation-alerts/04-02-SUMMARY.md` when done
</output>
