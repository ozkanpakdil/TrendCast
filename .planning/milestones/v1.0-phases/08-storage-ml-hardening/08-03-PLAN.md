---
phase: 08-storage-ml-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/services/engine/ml/transformers.ts
  - tests/unit/quantization-equivalence.test.ts
autonomous: true
requirements:
  - PERF-04
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 4
  confidence: med

must_haves:
  truths:
    - A shared `resolveDeviceAndDtype()` helper detects `navigator.gpu` and picks a dtype from the fallback chain `["q4", "q8", "fp16", "fp32"]` (D-04)
    - All five `get*Pipeline()` functions (embedding/sentiment/zero-shot/NER/LLM) use the shared helper and have a WebGPU→WASM catch-and-retry fallback (D-04)
    - The worker WASM path (`setWasmPath`/`deriveWasmPath`) still works when WebGPU is primary and WASM is fallback (D-04)
    - Quantized (q8/q4) correlation results are equivalent to fp32 within a tolerance (golden-test equivalence, D-05)
    - No Transformers.js v4 upgrade (D-06)
  artifacts:
    - src/services/engine/ml/transformers.ts (shared device/dtype resolution + fallback)
    - tests/unit/quantization-equivalence.test.ts
  key_links:
    - `get*Pipeline()` functions at src/services/engine/ml/transformers.ts:166-287
    - `getLLMPipeline` WebGPU+fallback pattern at transformers.ts:252-287 (the template to extend)
    - `TransformersLib.pipeline` options type already declares `device`/`dtype` (transformers.ts:48-66)
    - Worker WASM path setup at src/workers/ml-worker.ts:100-115
---

<objective>
Extend the WebGPU + WASM fallback pattern (currently LLM-only) to all ML pipelines, add an explicit dtype fallback chain, and prove quantized results are equivalent to fp32 via golden tests (PERF-04, D-04, D-05, D-06).

Purpose: Only the LLM pipeline currently uses WebGPU with WASM fallback; embedding/sentiment/zero-shot/NER are WASM-only. Extending the pattern covers all engines at the single choke point (`get*Pipeline()`), and the dtype chain picks the smallest available dtype. Golden tests catch silent quality drift from quantization.

Output: A shared `resolveDeviceAndDtype()` helper used by all five pipelines with WebGPU→WASM fallback, plus quantization-equivalence tests.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>
