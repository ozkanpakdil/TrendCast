---
phase: 05-market-driven-news
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/dashboard/hooks/useMarketNews.ts
  - src/dashboard/components/MarketDrivenNews.tsx
  - src/dashboard/App.tsx
autonomous: true
requirements:
  - MKT-01
user_setup: []

estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - The dashboard has a "📰 Market News" tab alongside feed/markets/news/correlations/watchlist/alerts/history/community/faq/settings (D-10)
    - The view is grouped by category (finance / politics / tech), each listing notable markets with correlated news + direction (D-09)
    - The view reads the read-only `marketNewsView` snapshot via a `useMarketNews` hook that mirrors `useAlerts` (D-11)
    - Rendering reuses `VirtualizedGrid` for the news lists within each category so large result sets don't regress responsiveness (D-12)
  artifacts:
    - src/dashboard/hooks/useMarketNews.ts
    - src/dashboard/components/MarketDrivenNews.tsx
    - 'market-news' tab wired into src/dashboard/App.tsx
  key_links:
    - useMarketNews reads CONFIG.storage.marketNewsView on mount + listens to chrome.storage.onChanged (D-11/D-13)
    - MarketDrivenNews imports CATEGORY_ORDER from @/config/taxonomy (not hardcoded — Pitfall 3)
    - MarketDrivenNews reuses VirtualizedGrid for per-category news lists (D-12)
---

<objective>
Add the "📰 Market News" dashboard tab: a `useMarketNews` hook that loads the derived `marketNewsView` snapshot and listens for storage changes, and a `MarketDrivenNews` component that renders the view grouped by category with direction badges, reusing `VirtualizedGrid`.

Purpose: MKT-01 requires the user-facing "market-driven news" view. D-09..D-12 lock the grouped-by-category layout, the new tab, the read-only snapshot hook, and `VirtualizedGrid` reuse. This plan is the browser/client rendering layer over the snapshot produced in 05-02.

Output: `src/dashboard/hooks/useMarketNews.ts`, `src/dashboard/components/MarketDrivenNews.tsx`, and the `market-news` tab wiring in `src/dashboard/App.tsx`.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/05-market-driven-news/05-CONTEXT.md
@.planning/phases/05-market-driven-news/05-RESEARCH.md

# Existing code this plan builds on
@src/dashboard/App.tsx
@src/dashboard/hooks/useAlerts.ts
@src/dashboard/hooks/useSnapshot.ts
@src/dashboard/components/AlertsTab.tsx
@src/dashboard/components/VirtualizedGrid.tsx
@src/dashboard/components/NewsFeed.tsx
@src/config/index.ts
@src/config/taxonomy.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useMarketNews hook</name>
  <files>src/dashboard/hooks/useMarketNews.ts</files>
  <action>
    Create `src/dashboard/hooks/useMarketNews.ts` mirroring `useAlerts` + `useSnapshot` (D-11). Export `useMarketNews()` returning `{ view, loading }`:
    - `view: MarketNewsView | null` (import `MarketNewsView` from `@/background/correlationNews`).
    - `loading: boolean`.
    - On mount, read `browser.storage.local.get(CONFIG.storage.marketNewsView)`; if a value exists, `setView(value)`; always `setLoading(false)` in `.finally()`.
    - Add a `browser.storage.onChanged` listener that, when `changes[CONFIG.storage.marketNewsView]?.newValue` is present, sets `view` to that value (the background writes the snapshot after correlation — D-11/D-13). Remove the listener on unmount.
    Use `@/messaging/browser` for `browser` and `@/config` for `CONFIG`. Do NOT add a new `Message` variant — the storage listener is more robust than a broadcast and survives missed broadcasts (RESEARCH.md Pattern 5).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>
    `useMarketNews()` loads the cached `marketNewsView` snapshot on mount and updates state on `chrome.storage.onChanged` for that key. Typecheck passes.
  </done>
  <reversibility rating="reversible">New hook, no contract — safe to adjust internals later.</reversibility>
</task>

<task type="task">
  <name>Task 2: Create MarketDrivenNews component + wire the tab into App.tsx</name>
  <files>src/dashboard/components/MarketDrivenNews.tsx, src/dashboard/App.tsx</files>
  <action>
    Create `src/dashboard/components/MarketDrivenNews.tsx` mirroring `AlertsTab` (read-only, theme-aware, memoized). Export `MarketDrivenNews` (memoized). Props: `{ view: MarketNewsView | null; loading: boolean; isDark: boolean }`.
    - If `loading`, show a "Loading market news…" placeholder.
    - If `view` is null or all categories are empty, show an empty state ("No market-driven news yet — run a correlation first.").
    - For each category in `CATEGORY_ORDER` (imported from `@/config/taxonomy` — do NOT hardcode the order or labels, Pitfall 3), render a section header (using `CATEGORY_RULES[cat].label`) + a `VirtualizedGrid` of market cards (D-12).
    - Each card shows: the market question, a direction badge (▲/▼/◆ reusing the `directionBadges` color contract from `AlertsTab` — map `'up'|'down'|'mixed'` to the same green/red/slate classes), the volume, and the top correlated news headlines (mirror the `NewsFeed` tile pattern).
    - Use `useMemo` to derive the per-category card arrays so re-renders are cheap.
    - Theme-aware classes via `isDark` (mirror `AlertsTab`'s `muted`/`card` helpers).

    Wire into `src/dashboard/App.tsx`:
    1. Import `useMarketNews` from `./hooks/useMarketNews` and `MarketDrivenNews` from `./components/MarketDrivenNews`.
    2. Add `'market-news'` to the `Tab` union (line ~54).
    3. Call `const { view: marketNewsView, loading: marketNewsLoading } = useMarketNews();` in the `App` component.
    4. Add `['market-news', '📰 Market News']` to the tab nav array (lines ~276-299), placed after `['alerts', '🔔 Alerts']`.
    5. Add `{activeTab === 'market-news' && (<section><h2 ...>📰 Market News</h2><MarketDrivenNews view={marketNewsView} loading={marketNewsLoading} isDark={isDark} /></section>)}` in the main content block, after the `alerts` section.
    Do NOT add any category keyword logic in the component — all taxonomy comes from `@/config/taxonomy`.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>
    `MarketDrivenNews` renders the grouped-by-category view with direction badges and `VirtualizedGrid` reuse. The `market-news` tab is added to the `Tab` union, nav array, and render block in `App.tsx`. Typecheck passes.
  </done>
  <reversibility rating="reversible">UI-only — tab and component can be adjusted without contract impact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage snapshot → React render | derived data rendered via React (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | `MarketDrivenNews` render | low | mitigate | Headlines are already sanitized at collection; React auto-escapes text — never use `dangerouslySetInnerHTML` |
| T-05-06 | DoS | dashboard responsiveness | medium | mitigate | `VirtualizedGrid` reuse (D-12) keeps large per-category lists responsive (PERF-01) |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase — all work reuses the existing stack. No package legitimacy gate required. |
</threat_model>

<verification>
- `bun run typecheck` passes with the new hook + component + tab wiring.
- `grep -n "'market-news'" src/dashboard/App.tsx` shows the tab in the `Tab` union, the nav array, and the render section.
- `grep -n "CATEGORY_ORDER" src/dashboard/components/MarketDrivenNews.tsx` confirms the taxonomy is imported, not hardcoded.
</verification>

<success_criteria>
- The dashboard has a "📰 Market News" tab that renders the grouped-by-category market-driven news view.
- The view reads the read-only `marketNewsView` snapshot via `useMarketNews` and updates on storage changes.
- Rendering reuses `VirtualizedGrid` so large result sets don't regress responsiveness.
</success_criteria>

<output>
Create `.planning/phases/05-market-driven-news/05-03-SUMMARY.md` when done
</output>
