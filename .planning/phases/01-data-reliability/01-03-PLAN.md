---
phase: 01-data-reliability
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/dashboard/components/SourceHealthIndicator.tsx
  - src/dashboard/App.tsx
  - tests/e2e/dashboard.spec.ts
  - tests/e2e/fixtures.ts
autonomous: true
requirements:
  - REL-01
  - REL-02
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The SourceHealthIndicator covers all 7 UI states: empty, loading, error, populated, partial, overflow, zero-one-many (UI-SPEC)."
    - "A source with no SourceHealthEntry renders as a neutral 'No data' badge (never omitted, never mislabeled)."
    - "The e2e test asserts the health indicator renders 'fetched N · correlated M' in the dashboard."
  artifacts:
    - src/dashboard/components/SourceHealthIndicator.tsx (edge states)
    - src/dashboard/App.tsx (error/loading wiring)
    - tests/e2e/dashboard.spec.ts (health indicator assertion)
    - tests/e2e/fixtures.ts (MOCK_SNAPSHOT.sourceHealth)
  key_links:
    - "useSnapshot().loading / error → SourceHealthIndicator loading/error states"
    - "MOCK_SNAPSHOT.sourceHealth → e2e assertion of 'fetched N · correlated M'"
---

<objective>
Expand the `SourceHealthIndicator` from the healthy-path tracer to cover all 7 UI states (empty, loading, error, populated, partial, overflow, zero-one-many) and add e2e coverage asserting the health indicator renders in the dashboard.

Purpose: The tracer (plan 01-01) proved the data path on the healthy path. This plan hardens the UI against the edge states the UI-SPEC mandates, and locks the behavior with an e2e test so the REL-02 indicator is regression-guarded.
Output: A fully state-covered `SourceHealthIndicator` plus an e2e assertion.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-reliability/1-RESEARCH.md
@.planning/phases/01-data-reliability/01-PATTERNS.md
@.planning/phases/01-data-reliability/01-UI-SPEC.md
@.planning/phases/01-data-reliability/1-VALIDATION.md
@src/dashboard/components/SourceHealthIndicator.tsx
@src/dashboard/App.tsx
@src/dashboard/hooks/useSnapshot.ts
@src/dashboard/hooks/useCorrelations.ts
@tests/e2e/dashboard.spec.ts
@tests/e2e/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover the 7 UI states in SourceHealthIndicator</name>
  <files>
    src/dashboard/components/SourceHealthIndicator.tsx
    src/dashboard/App.tsx
  </files>
  <action>
    Expand `SourceHealthIndicator` (created in plan 01-01) to cover all 7 UI states from the UI-SPEC. Keep it a read-only derived projection; delegate staleness to `computeHealth()` from `src/utils/source-health.ts`.

    State coverage (UI-SPEC "UI Considerations"):
    - **empty**: no `sourceHealth` entries → render the documented copy "No health data available" + body "Run a collection to see per-source status."
    - **loading**: while `useSnapshot().loading` is true → render placeholder skeleton badges (neutral `bg-slate-800`/`bg-light-border` pulse) instead of empty copy.
    - **error**: snapshot read failure → render "Health data unavailable — check your connection and run collection again."
    - **populated**: 6 sources render as a `flex flex-wrap gap-2` row of badges, each with a status dot + label + "fetched N · correlated M" detail.
    - **partial**: a source with no `SourceHealthEntry` renders as the neutral "No data" badge (never omitted, never mislabeled).
    - **overflow**: badges wrap via `flex-wrap`; 6 sources fit one row at `lg` breakpoint, wrap gracefully at narrower widths.
    - **zero-one-many**: "fetched N · correlated M" uses numeric counts (handles 0, 1, many without pluralization bugs); 0 sources renders the empty state.

    In `src/dashboard/App.tsx`, wire the `loading` and `error` props from `useSnapshot()` (which exposes `loading`; add an `error` state to the hook if not present — the hook currently swallows read errors in a `catch` that only logs). Pass `loading={loading}` and `error={error}` to `<SourceHealthIndicator>` in both the news and correlations tabs.

    Color/spacing/typography per UI-SPEC: health states use `bull`/`amber`/`bear`/`neutral` (never brand-500 for health badges); spacing on the 8-point scale; label 11px semibold, body 12px.
  </action>
  <verify>
    <automated>bun run test -- --run tests/unit/source-health.test.ts</automated>
  </verify>
  <done>
    All 7 UI states are covered in `SourceHealthIndicator`; empty/loading/error copy matches the UI-SPEC Copywriting Contract; `App.tsx` passes `loading`/`error` from the snapshot hook.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add e2e assertion for the health indicator</name>
  <files>
    tests/e2e/fixtures.ts
    tests/e2e/dashboard.spec.ts
  </files>
  <action>
    Extend the e2e coverage so the REL-02 health indicator is regression-guarded.

    1. In `tests/e2e/fixtures.ts`, add a `sourceHealth` field to `MOCK_SNAPSHOT` (line ~20) with entries for at least two sources (e.g. `seekingalpha: { lastFetchedAt: Date.now(), itemCount: 10, consecutiveFailures: 0 }` and `investing: { lastFetchedAt: Date.now(), itemCount: 0, consecutiveFailures: 1 }`) so the indicator renders both a healthy and a degraded badge.

    2. In `tests/e2e/dashboard.spec.ts`, add a test following the existing `openDashboard(page, overrides)` + `page.locator(...).toContainText(...)` pattern (lines 15-30). Assert the `SourceHealthIndicator` renders "fetched N · correlated M" in the correlations tab (and/or news tab) — e.g. assert the page contains "fetched 10 · correlated" for the seekingalpha badge and "fetched 0 · correlated" for the investing badge.
  </action>
  <verify>
    <automated>bun run test:e2e</automated>
  </verify>
  <done>
    `MOCK_SNAPSHOT` includes `sourceHealth`; the e2e test asserts the health indicator renders "fetched N · correlated M" for at least one source.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage → dashboard | `sourceHealth` is read by the dashboard; it must be treated as untrusted data (validated against the `NewsSource` union). |
| test fixtures → dashboard | `MOCK_SNAPSHOT.sourceHealth` is injected into the e2e page; test-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-04 | Tampering | `SourceHealthIndicator` rendering | low | accept | Read-only projection; no user-writable path. Health-map keys validated against `NewsSource` union. |
| T-1-05 | Tampering | `MOCK_SNAPSHOT.sourceHealth` | low | accept | Test-only fixture; no production code path. |
| T-1-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed in this phase. No legitimacy gate required. |
</threat_model>

<verification>
- `bun run test:e2e` passes (health indicator assertion green).
- `bun run test:all` passes at wave merge.
- Typecheck confirms `SourceHealthIndicator` props (`loading`, `error`) are wired correctly.
</verification>

<success_criteria>
- All 7 UI states covered in `SourceHealthIndicator`.
- e2e test asserts the health indicator renders "fetched N · correlated M".
- `MOCK_SNAPSHOT` includes `sourceHealth`.
</success_criteria>

<output>
Create `.planning/phases/01-data-reliability/01-03-SUMMARY.md` when done
</output>
