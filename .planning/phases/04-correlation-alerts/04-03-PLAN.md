---
phase: 04-correlation-alerts
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/dashboard/App.tsx
  - src/dashboard/hooks/useAlerts.ts
  - src/dashboard/components/AlertsTab.tsx
  - src/popup/components/Settings.tsx
  - src/types/index.ts
autonomous: true
requirements: [ALERT-02]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A new dedicated 'Alerts' tab exists alongside feed/markets/news/correlations/watchlist/history/community/faq/settings (D-08)"
    - "The Alerts tab renders a read-only list of AlertRecord with direction badges (bull/bear/mixed) + top signal/news + relative timestamp (ALERT-02)"
    - "The Alerts tab has a 'Clear all' action that removes records and resets the badge (D-10)"
    - "The useAlerts hook loads cached alertHistory on mount and listens for ALERTS_UPDATED"
    - "The Settings UI has an alertsEnabled toggle + alertCooldownMinutes input"
    - "Empty/loading/error states render per the UI-SPEC copywriting contract"
  artifacts:
    - src/dashboard/hooks/useAlerts.ts
    - src/dashboard/components/AlertsTab.tsx
    - src/dashboard/App.tsx (Tab union + nav + rendering)
    - src/popup/components/Settings.tsx (Alerts Section)
  key_links:
    - "useAlerts → browser.storage.local alertHistory + ALERTS_UPDATED message"
    - "AlertsTab 'Clear all' → CLEAR_ALERTS message → background clearAlerts()"
  prohibitions:
    - "Do NOT make the Alerts tab a section inside the watchlist tab — it is a dedicated tab (D-08)"
    - "Do NOT use accent color for the 'Clear all' action — it is destructive/de-emphasized (UI-SPEC)"
    - "Do NOT render a numeric confidence threshold in the alert body (D-02)"
---

<objective>
Add the dashboard "Alerts" tab (D-08) with a read-only `AlertRecord` list + "Clear all" action (D-10), a `useAlerts` hook that loads cached `alertHistory` and listens for `ALERTS_UPDATED`, and the `alertsEnabled`/`alertCooldownMinutes` settings in the popup Settings UI. Direction badges (bull/bear/mixed) + top signal/news + relative timestamp per the UI-SPEC contract.

Purpose: Give the user a visible, in-dashboard surface for alerts (the permission-denied fallback surface from D-07) and a way to control alert behavior.
Output: `useAlerts` hook, `AlertsTab` component, App.tsx tab wiring, Settings.tsx Alerts section.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-correlation-alerts/04-CONTEXT.md
@.planning/phases/04-correlation-alerts/04-UI-SPEC.md
@.planning/phases/04-correlation-alerts/04-PATTERNS.md
@src/dashboard/App.tsx
@src/dashboard/hooks/useCorrelations.ts
@src/dashboard/components/Watchlist.tsx
@src/popup/components/Settings.tsx
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build useAlerts hook + AlertsTab component</name>
  <files>src/dashboard/hooks/useAlerts.ts, src/dashboard/components/AlertsTab.tsx</files>
  <read_first>src/dashboard/hooks/useCorrelations.ts (load-cached-on-mount :96-112, message-listener :150-175, sendMessage :210-230), src/dashboard/components/Watchlist.tsx (platformBadges :22-25, memo export), src/types/index.ts (AlertRecord)</read_first>
  <action>
    Create `src/dashboard/hooks/useAlerts.ts` exporting `useAlerts()` returning `{ alerts: AlertRecord[], loading: boolean, error: string | null, clearAlerts: () => Promise<void> }`. On mount, load cached `alertHistory` from `browser.storage.local.get(CONFIG.storage.alertHistory)` and set state if `Array.isArray`. Add a `browser.runtime.onMessage` listener for `ALERTS_UPDATED` that sets `alerts` from `payload.alerts`. `clearAlerts()` calls `sendMessage('CLEAR_ALERTS', {})` and clears local state.

    Create `src/dashboard/components/AlertsTab.tsx` (mirror `Watchlist.tsx` `memo` export + `AlertsTabImpl` naming). Render a read-only list of `AlertRecord` rows: a direction badge (bull `#16a34a` ▲ / bear `#dc2626` ▼ / mixed `#6b7280` ◆ per UI-SPEC color contract), the market `question`, `topSignalText`/`topNewsHeadline` (truncated to 2 lines with ellipsis), and a relative timestamp (e.g. "2h ago") with absolute tooltip. Render empty state "No alerts yet" + body, loading state "Loading alerts…", and error state "Couldn't load alerts." per the UI-SPEC copywriting contract. Add a de-emphasized destructive "Clear all" button in the tab header with a two-step inline confirm ("Clear all" → "Confirm clear?" on first click, reverts after 3s or on cancel) that calls `clearAlerts()`.
  </action>
  <verify>
    <automated>bun run test</automated>
  </verify>
  <acceptance_criteria>
    `useAlerts` loads cached history + listens for `ALERTS_UPDATED`; `AlertsTab` renders direction badges + top signal/news + relative timestamps with empty/loading/error states; "Clear all" two-step confirm calls `clearAlerts()`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire Alerts tab into App.tsx + add settings toggle</name>
  <files>src/dashboard/App.tsx, src/popup/components/Settings.tsx, src/types/index.ts</files>
  <read_first>src/dashboard/App.tsx (Tab union :52, tab nav :279-300, tab rendering :634-643, hook wiring :87), src/popup/components/Settings.tsx (Section + toggle pattern :300-315, collection-interval input :20-33), src/types/index.ts (ExtensionSettings, DEFAULT_SETTINGS)</read_first>
  <action>
    In `src/dashboard/App.tsx`: add `'alerts'` to the `Tab` union (:52); add `['alerts', '🔔 Alerts']` to the tab nav array (:279-300); add `const { alerts, loading: alertsLoading, error: alertsError, clearAlerts } = useAlerts();` alongside the existing `useCorrelations()` call (:87); add an `activeTab === 'alerts'` rendering block that renders `<AlertsTab alerts={alerts} loading={alertsLoading} error={alertsError} onClear={clearAlerts} />` inside the existing `<main className="max-w-7xl mx-auto px-6 py-6">` container.

    In `src/popup/components/Settings.tsx`: add an "Alerts" `Section` with an `alertsEnabled` checkbox (mirror the theme toggle pattern, `checked={settings.alertsEnabled}` → `onUpdate({ alertsEnabled: e.target.checked })`) and an `alertCooldownMinutes` number input (mirror the collection-interval input pattern, `value={settings.alertCooldownMinutes}` → `onUpdate({ alertCooldownMinutes: parseInt(e.target.value) })`), with helper copy per the UI-SPEC copywriting contract.

    Ensure `alertsEnabled`/`alertCooldownMinutes` are present in `ExtensionSettings` + `DEFAULT_SETTINGS` in `src/types/index.ts` (added in Plan 01; verify they exist).
  </action>
  <verify>
    <automated>bun run test</automated>
  </verify>
  <acceptance_criteria>
    "Alerts" tab appears in nav and renders `AlertsTab`; Settings shows `alertsEnabled` toggle + `alertCooldownMinutes` input; `ExtensionSettings`/`DEFAULT_SETTINGS` include both fields; full unit suite green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| storage → dashboard | `alertHistory` read from `chrome.storage.local`; untrusted if corrupted |
| dashboard → background | `CLEAR_ALERTS` message mutates `alertHistory` + badge |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-07 | Tampering | AlertsTab | low | mitigate | Read-only list; only mutation is `CLEAR_ALERTS` via the two-step confirm; validate `AlertRecord` shape before render |
| T-4-08 | DoS | useAlerts | low | accept | History capped at 100 upstream; list scrolls within container |
| T-4-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new runtime dependencies this phase; no package installs |
</threat_model>

<verification>
- `bun run test` (full unit suite) green
- Manual: open dashboard, verify Alerts tab renders direction badges + top signal/news + "Clear all" two-step confirm; verify Settings toggle + cooldown persist (see 04-VALIDATION.md)
</verification>

<success_criteria>
- Dedicated "Alerts" tab renders read-only `AlertRecord` list with direction badges + top signal/news + relative timestamps
- "Clear all" two-step confirm resets history + badge
- Settings exposes `alertsEnabled` toggle + `alertCooldownMinutes` input
- Empty/loading/error states per UI-SPEC
</success_criteria>

<output>
Create `.planning/phases/04-correlation-alerts/04-03-SUMMARY.md` when done
</output>
