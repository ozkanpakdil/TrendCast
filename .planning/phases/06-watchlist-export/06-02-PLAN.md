---
phase: 06-watchlist-export
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/dashboard/components/Watchlist.tsx
  - src/dashboard/App.tsx
  - tests/unit/watchlist-sort-filter.test.ts
autonomous: true
requirements:
  - DASH-01
user_setup: []

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - The watchlist can be sorted by `addedAt` (default) or `volume24h` (D-04)
    - The watchlist can be filtered by `platform` (polymarket / kalshi) (D-05)
    - Each watchlist entry shows a correlation-status badge: `none` or `has-correlation` with a direction (bull/bear/neutral) (D-07)
    - The sort/filter set is minimal — no price-delta/confidence sort, no has-correlation filter (D-06)
    - Watchlist rendering stays bounded (no unbounded DOM rows) so the dashboard doesn't regress (Pitfall 8)
  artifacts:
    - sort + filter controls in src/dashboard/components/Watchlist.tsx
    - correlation-status badge in src/dashboard/components/Watchlist.tsx
    - correlation data passed to Watchlist from src/dashboard/App.tsx
    - tests/unit/watchlist-sort-filter.test.ts
  key_links:
    - `volume24h` sort reads from the live `markets` prop matched by contractId + platform (D-04)
    - `platform` filter reads `entry.platform` (D-05)
    - correlation badge derives "has correlation" from the `correlations` snapshot passed from App.tsx (D-07)
---

<objective>
Add sort controls (`addedAt` default, `volume24h`), filter controls (`platform`), and a per-market correlation-status badge to the watchlist, reusing the existing alert direction logic.

Purpose: DASH-01 requires the user to sort/filter the watchlist and see correlation status at a glance. D-04..D-07 lock the sort/filter set (minimal) and the correlation badge (none / has-correlation with direction). This plan is the client-side rendering layer over the backfilled watchlist from 06-01.

Output: sort + filter controls and a correlation-status badge in `Watchlist.tsx`, correlation data passed from `App.tsx`, and `tests/unit/watchlist-sort-filter.test.ts`.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/06-watchlist-export/06-CONTEXT.md
@.planning/phases/06-watchlist-export/06-RESEARCH.md

# Existing code this plan builds on
@src/dashboard/components/Watchlist.tsx
@src/dashboard/App.tsx
@src/dashboard/components/AlertsTab.tsx
@src/dashboard/components/MarketDrivenNews.tsx
@src/dashboard/hooks/useCorrelations.ts
@src/types/index.ts
@tests/unit/fixtures.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure sort/filter + correlation-status helpers + unit tests</name>
  <files>src/dashboard/utils/watchlistView.ts, tests/unit/watchlist-sort-filter.test.ts</files>
  <behavior>
    - Test 1 (sort addedAt, D-04): `sortWatchlist(entries, 'addedAt')` returns newest-first (descending addedAt)
    - Test 2 (sort volume24h, D-04): `sortWatchlist(entries, 'volume24h', markets)` sorts by the live market's `volume24h` descending; entries with no live market (or no volume) sort last
    - Test 3 (filter platform, D-05): `filterWatchlist(entries, 'polymarket')` returns only polymarket entries; `'all'` returns all
    - Test 4 (correlation status, D-07): `correlationStatusFor(contractId, platform, correlations)` returns `'none'` when the contract appears in neither `matches` nor `newsMatches`; returns `'has-correlation'` when it appears in either
    - Test 5 (direction, D-07): given a contract with a correlation, the direction is derived from the correlated signal/news sentiment + Yes-price (bull/bear/neutral)
  </behavior>
  <action>
    Create `src/dashboard/utils/watchlistView.ts` as a pure, unit-testable module for the watchlist view logic (D-04, D-05, D-07). Export:
    - `export type WatchlistSort = 'addedAt' | 'volume24h';`
    - `export type WatchlistPlatformFilter = 'all' | MarketPlatform;`
    - `export type CorrelationStatus = 'none' | 'has-correlation';`
    - `export function sortWatchlist(entries: WatchlistEntry[], sort: WatchlistSort, markets: MarketContract[]): WatchlistEntry[]` — for `'addedAt'`, sort by `b.addedAt - a.addedAt` (newest first, current behavior). For `'volume24h'`, look up each entry's live market by `contractId + platform` in `markets`, sort by `volume24h` descending, and place entries with no live market / no volume last (stable).
    - `export function filterWatchlist(entries: WatchlistEntry[], platform: WatchlistPlatformFilter): WatchlistEntry[]` — `'all'` returns the input unchanged; otherwise filters by `entry.platform === platform`.
    - `export function correlationStatusFor(contractId: string, platform: MarketPlatform, correlations: CorrelationResult | null): CorrelationStatus` — returns `'has-correlation'` if the contract appears in `correlations.matches` (by `contract.id`) or `correlations.newsMatches` (by `contract.id`), else `'none'`. Handle a null `correlations` (no run yet) as `'none'`.
    - `export function correlationDirectionFor(contractId: string, platform: MarketPlatform, correlations: CorrelationResult | null): 'bull' | 'bear' | 'neutral'` — derive the direction from the correlated matches for that contract: aggregate the mean sentiment of the correlated signals/news and the Yes-price (reuse the `yesPriceOf` pattern from `alerts.ts`); positive sentiment + rising/above-0.5 Yes → `'bull'`, negative → `'bear'`, else `'neutral'`. If no correlation, return `'neutral'`.
    Do NOT put any React/JSX in this module — it is pure logic so it is unit-testable. Do NOT hardcode platform labels here (import `MarketPlatform` from `@/types`).
    Create `tests/unit/watchlist-sort-filter.test.ts` covering the behaviors above. Use `bun run test -- tests/unit/watchlist-sort-filter.test.ts` as the automated gate.
  </action>
  <verify>
    <automated>bun run test -- tests/unit/watchlist-sort-filter.test.ts</automated>
  </verify>
  <done>
    `src/dashboard/utils/watchlistView.ts` exports `WatchlistSort`, `WatchlistPlatformFilter`, `CorrelationStatus`, `sortWatchlist`, `filterWatchlist`, `correlationStatusFor`, and `correlationDirectionFor`. All unit tests pass (addedAt sort, volume24h sort, platform filter, correlation status, direction).
  </done>
  <reversibility rating="reversible">New pure module, no contract — safe to adjust sort/filter/direction logic later.</reversibility>
</task>

<task type="task">
  <name>Task 2: Add sort/filter controls + correlation badge to Watchlist.tsx + pass correlations from App.tsx</name>
  <files>src/dashboard/components/Watchlist.tsx, src/dashboard/App.tsx</files>
  <action>
    Extend the watchlist UI with sort/filter controls and a correlation-status badge (D-04, D-05, D-07):

    1. `src/dashboard/components/Watchlist.tsx`:
       - Change `WatchlistProps` to add `correlations: CorrelationResult | null` (import `CorrelationResult` from `@/types`).
       - Add local state: `const [sort, setSort] = useState<WatchlistSort>('addedAt');` and `const [platformFilter, setPlatformFilter] = useState<WatchlistPlatformFilter>('all');` (import from `@/dashboard/utils/watchlistView`).
       - Replace the current `sorted` memo (line ~80) with a `view` memo that applies `filterWatchlist` then `sortWatchlist` using the live `markets` prop: `const view = useMemo(() => sortWatchlist(filterWatchlist(watchlist, platformFilter), sort, markets), [watchlist, platformFilter, sort, markets]);`.
       - Render a controls row above the list: a sort `<select>` with options `addedAt` ("Newest first") and `volume24h` ("Volume"), and a platform filter `<select>` with options `all` ("All platforms"), `polymarket`, `kalshi`. Style with the existing `card-hover`/`bg-slate-900` theme.
       - In each entry card, add a correlation-status badge next to the platform badge: call `correlationStatusFor(entry.contractId, entry.platform, correlations)`. If `'has-correlation'`, call `correlationDirectionFor(...)` and render a badge using the `directionBadges` color contract from `AlertsTab` (map `'bull'`→green `▲`, `'bear'`→red `▼`, `'neutral'`→slate `◆`). If `'none'`, render a muted "no correlation" badge. Reuse the `directionBadges` styling pattern (do NOT duplicate the alert direction logic — the pure helpers in `watchlistView.ts` own it).
       - Keep the list rendering bounded — the watchlist is typically small, so the existing `.map()` is fine; do NOT add unbounded DOM rows (Pitfall 8). If the watchlist grows large, the existing simple list is acceptable for this phase (research confirms watchlist is typically small).

    2. `src/dashboard/App.tsx`:
       - Pass the correlations to the Watchlist: change `<Watchlist markets={snapshot?.markets ?? []} />` (line ~647) to `<Watchlist markets={snapshot?.markets ?? []} correlations={correlations} />`. The `correlations` value already comes from `useCorrelations()` (line ~90).
    Do NOT add a new `Message` variant — the correlation data is already loaded client-side via `useCorrelations`.
  </action>
  <verify>
    <automated>bun run test -- tests/unit/watchlist-sort-filter.test.ts && bun run typecheck</automated>
  </verify>
  <done>
    `Watchlist.tsx` has sort (addedAt/volume24h) and filter (platform) controls, and a per-entry correlation-status badge (none / has-correlation with direction). `App.tsx` passes `correlations` to the Watchlist. Typecheck passes.
  </done>
  <reversibility rating="reversible">UI-only — controls and badge can be adjusted without contract impact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| correlations snapshot → correlation badge | derived correlation status rendered via React (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | correlation badge render | low | mitigate | React auto-escapes text — never use `dangerouslySetInnerHTML`; badge text is derived from enum values, not user input |
| T-06-04 | DoS | watchlist rendering | medium | mitigate | Watchlist stays a bounded `.map()` over a typically-small list; no unbounded DOM rows added (Pitfall 8) |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase — all work reuses the existing stack. No package legitimacy gate required. |
</threat_model>

<verification>
- `bun run test -- tests/unit/watchlist-sort-filter.test.ts` passes (addedAt sort, volume24h sort, platform filter, correlation status, direction).
- `bun run typecheck` passes with the new module + Watchlist/App wiring.
- `grep -n 'correlations=' src/dashboard/App.tsx` shows the Watchlist receives the correlations prop.
</verification>

<success_criteria>
- The watchlist can be sorted by `addedAt` (default) or `volume24h`, and filtered by `platform`.
- Each watchlist entry shows a correlation-status badge (none / has-correlation with direction).
- The sort/filter set is minimal (D-06) and rendering stays bounded (Pitfall 8).
</success_criteria>

<output>
Create `.planning/phases/06-watchlist-export/06-02-SUMMARY.md` when done
</output>
