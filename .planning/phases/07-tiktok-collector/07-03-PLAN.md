---
phase: 07-tiktok-collector
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - src/services/collectors/tiktok.ts
  - src/services/collectors/index.ts
  - src/background/index.ts
  - src/manifest.config.ts
  - src/dashboard/App.tsx
  - tests/unit/tiktok-collector.test.ts
autonomous: false
requirements:
  - SRC-01
  - SRC-02
user_setup: []

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - TikTok collects trending topics AUTOMATICALLY (like Reddit/X) via a pure background `fetch()` of the discover page — there is NO URL-paste input box in the dashboard and NO visible tab (D-03)
    - The background fetches `CONFIG.scrape.tiktok.url` via `conditionalFetch` (host_permissions let the MV3 worker fetch TikTok directly, bypassing CORS) and parses the embedded SSR JSON (D-03)
    - The automatic collector is wired into `runCollection()` via `if (enabled.tiktok)`, isolated so TikTok failure never degrades other sources (D-04)
    - The content script still handles the discover page when the user visits (broad selectors, graceful `[]`) and reports via `REPORT_SOCIAL_DATA`/`REPORT_SOCIAL_HEALTH` (D-01, D-02)
    - All sources (6 news + 2 market platforms + TikTok) render end-to-end in the dashboard and export (SRC-02)
  artifacts:
    - `collectTikTokTrends()` in src/services/collectors/tiktok.ts (automatic background-fetch collector)
    - `if (enabled.tiktok)` wiring in src/background/index.ts `runCollection()`
    - `*://*.tiktok.com/*` in the socials content-script `matches` (src/manifest.config.ts)
    - `SocialHealthBadge` mounted in src/dashboard/App.tsx (no URL input)
    - tests/unit/tiktok-collector.test.ts
  key_links:
    - `runCollection()` → `if (enabled.tiktok)` → `collectTikTokTrends()` → `conditionalFetch(discover)` → parse SSR JSON → `storeSignals` → dashboard/export (SRC-02)
    - Background fetch uses the existing `conditionalFetch` timeout/cache config (no tab, no `maxConcurrentTabs`)
---

<objective>
Wire TikTok into the automatic collection flow (like Reddit/X) via a pure background `fetch()` of the discover page, and verify all sources render end-to-end (D-03, SRC-02, success criteria 3 & 4).

Purpose: TikTok must bring trending topics automatically, exactly like Reddit and X — there is NO manual URL-paste input box in the dashboard (the user explicitly rejected that flow as over-engineered) and NO visible tab (the user rejected the background-tab approach). D-03 mandates an automatic background-fetch collector: `collectTikTokTrends()` fetches the discover page via `conditionalFetch` (host_permissions let the MV3 worker fetch TikTok directly, bypassing CORS), parses the embedded SSR JSON, and normalises to `SocialSignal`. SRC-02 requires confirming all sources (news + markets + TikTok) render end-to-end in dashboard/export.

Output: `collectTikTokTrends()` automatic collector + `runCollection()` wiring, the manifest `matches` fix for `www.tiktok.com`, the `SocialHealthBadge` mounted in the dashboard (no URL input), and unit tests.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-tiktok-collector/07-CONTEXT.md
@.planning/phases/07-tiktok-collector/07-RESEARCH.md
@.planning/phases/07-tiktok-collector/07-01-SUMMARY.md
@.planning/phases/07-tiktok-collector/07-02-SUMMARY.md

# Existing code this plan builds on
@src/types/index.ts
@src/config/index.ts
@src/background/index.ts
@src/services/collectors/tiktok.ts
@src/services/collectors/x-trends.ts
@src/content/socials/index.ts
@src/dashboard/App.tsx
@src/dashboard/components/SocialHealthBadge.tsx
@src/utils/export.ts
@tests/unit/fixtures.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Automatic background-fetch collector (collectTikTokTrends) + unit tests</name>
  <files>src/services/collectors/tiktok.ts, tests/unit/tiktok-collector.test.ts</files>
  <behavior>
    - Test 1: `collectTikTokTrends()` calls `fetch(CONFIG.scrape.tiktok.url, ...)` (the discover page) — no `browser.tabs.create`
    - Test 2: trending titles are parsed from the embedded `__UNIVERSAL_DATA_FOR_REHYDRATION__` SSR JSON
    - Test 3: repeated titles are deduped
    - Test 4: falls back to `#hashtag` scanning when SSR JSON is absent
    - Test 5: returns `[]` gracefully when nothing parseable (SRC-01)
    - Test 6: caps at MAX_TRENDS (30)
    - Test 7: returns `[]` on 304 Not Modified (conditionalFetch cache)
    - Test 8: throws on non-ok, non-304 responses
    - Test 9: normalises parsed titles to `SocialSignal` (platform 'tiktok', rank virality, sentiment, keywords, search URL)
  </behavior>
  <action>
    Implement the automatic background-fetch collector (D-03). TikTok has no public API, but the discover page embeds trending data as SSR JSON in a `__UNIVERSAL_DATA_FOR_REHYDRATION__` script tag. Because `https://*.tiktok.com/*` is in `host_permissions`, the MV3 worker can `fetch()` it directly (extension workers with host permissions are NOT subject to CORS).

    1. `src/services/collectors/tiktok.ts` — implement `collectTikTokTrends(): Promise<SocialSignal[]>`:
       - `conditionalFetch(CONFIG.scrape.tiktok.url, { headers: { Accept: 'text/html', 'User-Agent': ... } })` (reuses the existing timeout + ETag cache).
       - If `null` (304), return `[]`.
       - If `!response.ok`, throw.
       - `await response.text()`, then `parseTrendTitles(html)`.
       - Map titles → `normaliseTikTokTrend(title, idx)` → filter empty.
       Import `browser`-free helpers: `conditionalFetch` from `@/utils/conditional-fetch`, `CONFIG` from `@/config`, `analyzeSentiment`/`extractKeywords`.

    2. `parseTrendTitles(html)` — extract the SSR JSON blob, recursively walk it for objects with a string `title` + `challengeId`/`id`, dedup, cap at MAX_TRENDS. Fall back to `#hashtag` regex scanning when the JSON shape changes. Return `[]` when nothing found.

    3. Create `tests/unit/tiktok-collector.test.ts` — mock `@/messaging/browser` (for `conditionalFetch`'s storage cache) and `globalThis.fetch`; verify the behaviors above. Use `bun run test -- tests/unit/tiktok-collector.test.ts` as the automated gate.
  </action>
  <verify>
    <automated>bun run test -- tests/unit/tiktok-collector.test.ts</automated>
  </verify>
  <done>
    `collectTikTokTrends()` fetches the discover page in the background (no tab), parses SSR JSON, dedups, caps at 30, handles 304, and never throws uncaught. All unit tests pass.
  </done>
  <reversibility rating="reversible">New collector module — additive; no change to existing handlers.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Wire into runCollection + fix manifest matches</name>
  <files>src/background/index.ts, src/services/collectors/index.ts, src/manifest.config.ts</files>
  <action>
    Wire the automatic collector into the collection cycle and fix the manifest so the content script actually runs on TikTok (D-03, D-01):

    1. `src/services/collectors/index.ts` — export `collectTikTokTrends` from `./tiktok`.

    2. `src/background/index.ts` — in `runCollection()`, after the existing `enabled.x` block, add:
       `if (enabled.tiktok) { tasks.push(collectTikTokTrends().then((signals) => storeSignals(signals)).catch(...)) }`
       so TikTok is collected automatically on the same cycle as Reddit/X. Import `collectTikTokTrends` from `@/services/collectors`. The `.catch` isolates TikTok failure so it never degrades other sources (D-04).

    3. `src/manifest.config.ts` — the socials content-script `matches` currently has `'*://tiktok.com/*'`, but TikTok redirects to `www.tiktok.com`, so the scraper never runs. Change it to `'*://*.tiktok.com/*'`. Also add `'*://*.tiktok.com/*'` to the `web_accessible_resources` matches for the socials group. (`https://*.tiktok.com/*` host_permission is already present — it enables the background fetch.)
  </action>
  <verify>
    <automated>bun run test -- tests/unit/tiktok-collector.test.ts && bun run typecheck</automated>
  </verify>
  <done>
    `runCollection()` collects TikTok automatically via `collectTikTokTrends()` when `enabled.tiktok`; the manifest matches `www.tiktok.com` so the content script runs. Typecheck passes.
  </done>
  <reversibility rating="reversible">Additive wiring + manifest match fix — no stored-data contract.</reversibility>
</task>

<task type="checkpoint:human-verify">
  <name>Checkpoint: Verify automatic TikTok collection + all sources render end-to-end</name>
  <action>
    Manually verify the automatic collection and SRC-02 end-to-end wiring in a real browser:

    1. Mount the `SocialHealthBadge` from 07-02 into `src/dashboard/App.tsx` so the TikTok health state is visible. There is NO URL-paste input — TikTok collects automatically.
    2. Load the extension (TikTok is on by default, like Reddit/X) and trigger a collection cycle.
    3. Confirm: TikTok trends are fetched in the background (NO visible tab opens), signals appear in the dashboard HypeFeed with the 🎵 TikTok icon.
    4. Confirm the TikTok health badge updates (healthy on success; degraded/no-data when unreachable).
    5. Confirm SRC-02: all sources render end-to-end — 6 news sources, Polymarket + Kalshi markets, and TikTok signals all appear in the dashboard, and `src/utils/export.ts` exports TikTok signals generically by `platform`.
    6. Confirm TikTok failure does NOT degrade other sources (success criterion 2): disable network to tiktok.com and verify BBC/CNN/Polymarket/Kalshi still collect.
  </action>
  <verify>
    <human-check>
      User confirms: (a) TikTok trending topics are collected automatically (no URL-paste input, no visible tab) and signals appear in the dashboard; (b) the TikTok health badge shows graceful degradation when unreachable; (c) all sources (news + markets + TikTok) render in dashboard and export; (d) TikTok failure does not degrade other sources.
    </human-check>
  </verify>
  <done>
    User has confirmed automatic TikTok collection works, the TikTok health badge degrades gracefully, all sources render end-to-end (SRC-02), and TikTok isolation holds (success criteria 2, 3, 4).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background → tiktok.com | background fetches the discover page HTML for automatic collection |
| content script → background REPORT_SOCIAL_DATA/HEALTH | scraped signals + health cross the content/background boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | DoS | background fetch | medium | mitigate | `conditionalFetch` applies `CONFIG.fetch.timeoutMs` abort; `Promise.allSettled` in `runCollection` isolates TikTok failure |
| T-07-07 | Info Disclosure | fetched page content | low | accept | Fetches the public discover page in the user's own browser session; 100% client-side, no data leaves the browser |
| T-07-08 | Tampering | `scrapeTikTok` DOM input | medium | mitigate | Read-only DOM text extraction; no `innerHTML`/`eval`; broad selectors return `[]` on no-match; try/catch so DOM changes never break the host page |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this plan — reuses existing stack. No package legitimacy gate required. |
</threat_model>

<verification>
- `bun run test -- tests/unit/tiktok-collector.test.ts` passes (fetch discover page, parse SSR JSON, dedup, #hashtag fallback, graceful [], 304 skip, non-ok throw, MAX_TRENDS cap).
- `bun run typecheck` passes with the new collector + wiring.
- `grep -n 'enabled.tiktok' src/background/index.ts` shows TikTok wired into `runCollection` (D-03).
- `grep -n 'collectTikTokTrends' src/background/index.ts src/services/collectors/tiktok.ts` shows the automatic collector (D-03).
- `grep -n 'tiktok.com' src/manifest.config.ts` shows `*://*.tiktok.com/*` in the socials matches.
- Human checkpoint confirms automatic collection + SRC-02 end-to-end + TikTok isolation.
</verification>

<success_criteria>
- TikTok collects trending topics automatically via a background tab to the discover page (like Reddit/X) — no URL-paste input box (D-03).
- The automatic collector is wired into `runCollection()` via `if (enabled.tiktok)` and isolated so TikTok failure never degrades other sources.
- The content script handles the discover page (broad selectors, graceful `[]`).
- The `SocialHealthBadge` is mounted in the dashboard and degrades gracefully when unreachable.
- All sources (6 news + 2 markets + TikTok) render end-to-end in dashboard and export (SRC-02).
</success_criteria>

<output>
Create `.planning/phases/07-tiktok-collector/07-03-SUMMARY.md` when done
</output>
