---
phase: 09-news-source-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - tests/unit/settings-deep-merge.test.ts
  - tests/unit/settings-migration.test.ts
autonomous: true
requirements:
  - NEWS-03
estimate:
  tokens: 35000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Regression unit tests prove the deep-merge + migration behavior (NEWS-03)"
    - "The deep-merge test proves an existing user's partial enabledSources yields seekingalpha/investing/googleFinance = true"
    - "The deep-merge test proves an explicit user preference (e.g. seekingalpha: false) is preserved"
    - "The migration test proves missing source flags are backfilled and persisted, and that the migration is idempotent"
    - "The full unit suite still passes (no regressions in existing tests)"
  artifacts:
    - tests/unit/settings-deep-merge.test.ts (extended)
    - tests/unit/settings-migration.test.ts (extended)
  key_links:
    - "The deep-merge test imports deepMergeSettings from @/utils/settings"
    - "The migration test imports migrateEnabledSources from @/utils/settings"
---

<objective>
Complete and harden the regression unit-test coverage for the deep-merge + migration fix.

Purpose: Fixes NEWS-03. Plans 09-01 and 09-02 create the helpers and their initial tests; this plan extends that coverage to the full regression matrix required by the success criteria — proving the deep-merge defaults newer source flags to `true`, preserves explicit preferences, and that the migration backfills missing flags idempotently. It also runs the full unit suite to confirm no regressions.

Output: Extended `tests/unit/settings-deep-merge.test.ts` and `tests/unit/settings-migration.test.ts` covering the complete regression matrix, verified against the full unit suite.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-news-source-fix/09-UI-SPEC.md
@src/utils/settings.ts
@tests/unit/settings-deep-merge.test.ts
@tests/unit/settings-migration.test.ts
@tests/unit/watchlist-migration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the deep-merge regression test to the full matrix</name>
  <files>tests/unit/settings-deep-merge.test.ts</files>
  <read_first>
    - tests/unit/settings-deep-merge.test.ts — the test file created in plan 09-01 (extend it)
    - src/utils/settings.ts — the `deepMergeSettings` helper under test
    - src/types/index.ts — `ExtensionSettings` and `DEFAULT_SETTINGS` for building realistic stored fixtures
  </read_first>
  <action>
    Extend `tests/unit/settings-deep-merge.test.ts` to cover the full regression matrix beyond the initial 5 cases. Add cases proving: (1) a stored `enabledSources` that is a partial object with only `bbc: false` yields `bbc: false` preserved AND `seekingalpha`/`investing`/`googleFinance`/`cnn`/`yahoo` all `true` (mixed explicit-off + default-on); (2) the returned object is a NEW reference (does not mutate `defaults` or `stored` — assert `result !== defaults` and `result.enabledSources !== defaults.enabledSources`); (3) a stored `enabledSources` with `seekingalpha: false` and `investing: false` keeps both `false` (explicit user preference wins over default `true`); (4) top-level scalar fields from stored (e.g. `collectionIntervalMinutes: 30`, `theme: 'light'`) override defaults while unspecified top-level fields (e.g. `maxHistoryEntries`) keep defaults. Use `DEFAULT_SETTINGS` as the `defaults` argument and build stored fixtures as `Partial<ExtensionSettings>` literals. Do not mock `@/messaging/browser` — the helper is pure.
  </action>
  <verify>
    <automated>bun run test -- tests/unit/settings-deep-merge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test file contains cases for all 4 added scenarios above plus the original 5
    - Every case asserts concrete output values (not just "does not throw")
    - `bun run test -- tests/unit/settings-deep-merge.test.ts` passes
  </acceptance_criteria>
  <done>The deep-merge regression matrix proves NEWS-01 behavior: newer source flags default to true, explicit preferences are preserved, and the merge is non-mutating.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the migration regression test and run the full unit suite</name>
  <files>tests/unit/settings-migration.test.ts</files>
  <read_first>
    - tests/unit/settings-migration.test.ts — the test file created in plan 09-02 (extend it)
    - src/utils/settings.ts — the `migrateEnabledSources` helper under test
    - src/types/index.ts — `ExtensionSettings` and `DEFAULT_SETTINGS`
  </read_first>
  <action>
    Extend `tests/unit/settings-migration.test.ts` to cover the full migration regression matrix beyond the initial 5 cases. Add cases proving: (1) the backfilled result preserves ALL stored keys (e.g. `collectionIntervalMinutes`, `theme`, `redditSubreddits`) and only adds the missing source flags; (2) running `migrateEnabledSources` on the result of a previous migration returns `null` (idempotent — second run is a no-op); (3) a stored `enabledSources` missing only `googleFinance` (but having `seekingalpha`/`investing`) backfills just `googleFinance` and leaves the present flags untouched; (4) the returned object is a NEW reference (does not mutate `stored`). Then run the FULL unit suite (`bun run test`) to confirm no regressions in existing tests, and `bun run typecheck`.
  </action>
  <verify>
    <automated>bun run test && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The test file contains cases for all 4 added scenarios above plus the original 5
    - `bun run test` passes (full unit suite, no regressions)
    - `bun run typecheck` passes
  </acceptance_criteria>
  <done>The migration regression matrix proves NEWS-02 behavior (backfill, preservation, idempotency, non-mutation) and the full unit suite passes with no regressions (NEWS-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → helper under test | Test inputs are developer-authored; no untrusted boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering | test fixtures | low | accept | Tests are developer-authored and run in a sandboxed Vitest environment; no untrusted input crosses a boundary. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages are installed in this phase; the package-legitimacy gate is not triggered. |
</threat_model>

<verification>
- `bun run test -- tests/unit/settings-deep-merge.test.ts` passes
- `bun run test -- tests/unit/settings-migration.test.ts` passes
- `bun run test` (full unit suite) passes with no regressions
- `bun run typecheck` passes
</verification>

<success_criteria>
- The deep-merge regression matrix proves NEWS-01 (newer flags default true, explicit preferences preserved, non-mutating)
- The migration regression matrix proves NEWS-02 (backfill, preservation, idempotency, non-mutation)
- The full unit suite passes with no regressions (NEWS-03)
</success_criteria>

<output>
Create `.planning/phases/09-news-source-fix/09-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `tests/unit/settings-deep-merge.test.ts` — extended to the full deep-merge regression matrix
- `tests/unit/settings-migration.test.ts` — extended to the full migration regression matrix
