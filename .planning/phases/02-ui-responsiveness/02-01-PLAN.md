---
phase: 02-ui-responsiveness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/dashboard/components/VirtualizedGrid.tsx
  - src/dashboard/components/HypeFeed.tsx
  - src/dashboard/components/NewsFeed.tsx
  - tests/unit/virtual-grid.test.ts
  - tests/e2e/dashboard.spec.ts
autonomous: true
requirements:
  - PERF-01
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Empty arrays render the documented empty-state copy via early return before the virtualizer (HypeFeed: 'No social signals collected yet.' / NewsFeed: 'No news collected yet.')."
    - "Large datasets render only visible rows in the DOM (virtualizer count = rows.length; overscan: 3 for smooth pre-scroll)."
    - "Variable-height cards are handled by measureElement + data-index; partial rows render normally with line-clamp truncation."
    - "Virtualization bounds DOM to visible rows only; the scroll container is max-h-[70vh] overflow-y-auto giving the virtualizer a bounded viewport."
    - "Single item renders as one row of 1 card; many items render only visible rows (virtualizer count = rows.length)."
    - "line-clamp-2 on headlines and line-clamp-1 on news summary are preserved unchanged; measureElement corrects variable row heights."
    - "Card styling, grid appearance, empty-state copy, and interaction behavior are preserved (brownfield 'keep it identical' contract)."
    - "MarketOdds (treemap) and CorrelationPanel (graph) are untouched (D-02)."
    - "React.memo is applied only to HypeFeed and NewsFeed; no other components memoized, no App.tsx state splitting (D-03)."
    - "The virtualizer's measureElement + data-index corrects variable-height row estimates (120px estimate)."
  artifacts:
    - src/dashboard/components/VirtualizedGrid.tsx
    - src/dashboard/components/HypeFeed.tsx
    - src/dashboard/components/NewsFeed.tsx
    - tests/unit/virtual-grid.test.ts
    - tests/e2e/dashboard.spec.ts
    - package.json
    - bun.lock
  key_links:
    - "VirtualizedGrid ↔ HypeFeed/NewsFeed: the shared helper must preserve the exact grid classes and card markup."
    - "measureElement + data-index ↔ variable row heights: missing data-index silently breaks measurement."
    - "getColumnCount breakpoints ↔ Tailwind grid-cols-* classes: must match to avoid scroll jumps / wrap mismatch."
---

## Phase Goal

**As a** TrendCast user, **I want to** scroll through the news and social feeds without lag when large datasets are loaded, **so that** I can interact with the dashboard without the UI freezing during rendering.

## Objective

Virtualize the two list feeds (`HypeFeed`, `NewsFeed`) using **@tanstack/react-virtual** so only visible rows are in the DOM. This eliminates the jank source (hundreds of DOM nodes rendered at once) while preserving the exact grid appearance, empty-state copy, and interaction behavior.

**Purpose:** PERF-01 — user can interact with the dashboard without lag when rendering large datasets (scroll without jank, tab-switch without delay, click/filter/hover without freeze).

**Output:** A shared row-virtualized grid helper, both feeds rewired through it, and unit + e2e tests proving bounded DOM.

## Acceptance Criteria

- [ ] `@tanstack/react-virtual@^3.14.10` added to `package.json` dependencies via `bun add` (Bun only, never npm/npx)
- [ ] `HypeFeed` and `NewsFeed` render only visible rows; DOM node count is bounded with a large dataset
- [ ] Empty-state copy preserved via early return before the virtualizer
- [ ] Card styling, grid appearance, and interaction behavior unchanged
- [ ] `MarketOdds`/`CorrelationPanel` untouched (D-02); no extra memoization or App.tsx state splitting (D-03)
- [ ] Unit tests for `chunk` + `getColumnCount`; e2e tests for bounded DOM + scroll reveal

## MVP Slice Tasks

<tasks>

<task type="tracer">
  <name>Task 1: Install @tanstack/react-virtual and wire HypeFeed through a shared row-virtualized grid</name>
  <files>package.json, bun.lock, src/dashboard/components/VirtualizedGrid.tsx, src/dashboard/components/HypeFeed.tsx, tests/unit/virtual-grid.test.ts, tests/e2e/dashboard.spec.ts</files>
  <read_first>
    - src/dashboard/components/HypeFeed.tsx (the feed being rewired — preserve card markup exactly)
    - src/dashboard/components/NewsFeed.tsx (reference for the identical card pattern)
    - tests/e2e/dashboard.spec.ts (existing `main .grid > *` assertions that must keep passing)
    - tests/e2e/fixtures.ts (MOCK_SNAPSHOT shape for seeding a large dataset)
    - 2-RESEARCH.md (Pattern 1 + Pattern 2: row-based virtualization, measureElement, estimateSize 120, overscan 3)
    - 02-UI-SPEC.md (empty-state copy, max-h-[70vh], grid-cols classes, card minHeight 100px)
  </read_first>
  <action>
    Install the dependency with Bun only: run `bun add @tanstack/react-virtual`. This adds `@tanstack/react-virtual@^3.14.10` to `package.json` `dependencies` and updates `bun.lock`. Never use npm/npx.

    Create `src/dashboard/components/VirtualizedGrid.tsx` exporting three symbols:
    - `getColumnCount(width: number): number` — pure breakpoint mapping matching Tailwind `grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6`: `width <= 639 → 2`, `<= 767 → 3`, `<= 1023 → 4`, `<= 1279 → 5`, else `6`.
    - `chunk<T>(arr: T[], size: number): T[][]` — pure chunk helper slicing `arr` into rows of `size`.
    - `VirtualizedGrid` — a `memo`-wrapped function component with props `{ items: React.ReactNode[] }`. Internally: a `useRef<HTMLDivElement>(null)` scroll container; a `useColumnCount` hook that reads `parentRef.current.clientWidth` via a `ResizeObserver` and maps it through `getColumnCount`; a `useMemo` that chunks `items` into rows via `chunk(items, cols)`; and a `useVirtualizer` from `@tanstack/react-virtual` with `count: rows.length`, `getScrollElement: () => parentRef.current`, `estimateSize: () => 120`, `overscan: 3`. Render a scroll container div `className="max-h-[70vh] overflow-y-auto"` with `ref={parentRef}`, an inner spacer div `style={{ height: \`${rowVirtualizer.getTotalSize()}px\`, position: 'relative' }}`, and for each virtual row a div with `key={virtualRow.key}`, `data-index={virtualRow.index}`, `ref={rowVirtualizer.measureElement}`, `className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-2"`, and `style={{ position: 'absolute', top: 0, left: 0, width: '100%', transform: \`translateY(${virtualRow.start}px)\` }}`, containing `rows[virtualRow.index]`. The empty state is NOT handled here — the parent early-returns before rendering `VirtualizedGrid`.

    Rewire `HypeFeedImpl` in `src/dashboard/components/HypeFeed.tsx`: keep the `useMemo` sort by virality descending and the empty-state early return (`if (sorted.length === 0) return <p className="text-slate-500 text-sm text-center py-8">No social signals collected yet. Visit X, Reddit, or TikTok to collect data, or wait for the next hourly collection.</p>`). Replace the `.map()` grid with `<VirtualizedGrid items={sorted.map((signal) => <card/>)} />`, preserving the exact card markup (heatColor, textColor, platformIcons, formatEngagement, isHot border-brand-500/40 + shadow, `minHeight: '100px'`, line-clamp-2 headline, keyword chips, author/engagement). Keep `export const HypeFeed = memo(HypeFeedImpl)`.

    Add unit tests in `tests/unit/virtual-grid.test.ts` for the pure functions: `chunk` (empty array → `[]`, exact-size split, remainder row) and `getColumnCount` (boundary widths 639/640, 767/768, 1023/1024, 1279/1280, and a large width → 6).

    Add e2e tests in `tests/e2e/dashboard.spec.ts`: seed a snapshot with ~200 signals (build an array of `SocialSignal` fixtures in the test, reusing the MOCK_SNAPSHOT signal shape), assert `page.locator('main .grid > *')` count is well below 200 (e.g. `< 60`), and assert that scrolling the feed container reveals more cards (count increases or a later item becomes visible). Keep the existing `main .grid > *` count-3 and empty-state assertions passing.
  </action>
  <verify>
    <automated>bun run test && bunx playwright test tests/e2e/dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test` passes (virtual-grid.test.ts: chunk + getColumnCount cases green)
    - `bunx playwright test tests/e2e/dashboard.spec.ts` passes: existing feed tests (count 3, empty state, sort order) AND new bounded-DOM + scroll-reveal tests green
    - `@tanstack/react-virtual@^3.14.10` present in `package.json` `dependencies`
    - `HypeFeed` renders only visible rows; `main .grid > *` count < 60 with 200 signals
  </acceptance_criteria>
  <done>HypeFeed is virtualized end-to-end through the shared `VirtualizedGrid`; unit + e2e tests green; dependency installed via Bun.</done>
</task>

<task type="auto">
  <name>Task 2: Wire NewsFeed through the shared row-virtualized grid</name>
  <files>src/dashboard/components/NewsFeed.tsx, tests/e2e/dashboard.spec.ts</files>
  <read_first>
    - src/dashboard/components/NewsFeed.tsx (the feed being rewired — preserves card markup exactly)
    - src/dashboard/components/VirtualizedGrid.tsx (the shared helper created in Task 1)
    - tests/e2e/dashboard.spec.ts (existing News tab tests + empty-state test)
  </read_first>
  <action>
    Rewire `NewsFeedImpl` in `src/dashboard/components/NewsFeed.tsx`: keep the `useMemo` sort by `publishedAt` descending and the empty-state early return (`if (sorted.length === 0) return <p className="text-slate-500 text-sm text-center py-8">No news collected yet. Wait for the next hourly collection.</p>`). Replace the `.map()` grid with `<VirtualizedGrid items={sorted.map((item) => <a/>)} />`, preserving the exact card markup (sourceColor, textColor, sourceLabels, formatTime, `minHeight: '100px'`, line-clamp-2 headline, line-clamp-1 summary, `href`/`target="_blank"`/`rel="noopener noreferrer"`). Keep `export const NewsFeed = memo(NewsFeedImpl)`.

    Add e2e tests in `tests/e2e/dashboard.spec.ts`: seed a snapshot with ~200 news items (reusing the MOCK_SNAPSHOT news shape), switch to the News tab, assert `main .grid > *` count is well below 200 (e.g. `< 60`), and assert the empty-state test (`No news collected yet`) still passes.
  </action>
  <verify>
    <automated>bun run test && bunx playwright test tests/e2e/dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx playwright test tests/e2e/dashboard.spec.ts` passes: News tab bounded-DOM test, empty-state test, and existing News assertions green
    - `bun run test` still green (no regression from Task 1)
    - `NewsFeed` renders only visible cards; `main .grid > *` count < 60 with 200 news items
  </acceptance_criteria>
  <done>NewsFeed is virtualized through the shared `VirtualizedGrid`; empty state preserved; e2e + unit green.</done>
</task>

</tasks>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DOM | The dashboard renders typed snapshot data into the virtualized grid. No user input crosses this boundary; the virtualizer is pure DOM measurement. |
| npm registry → node_modules | `@tanstack/react-virtual` is installed as a new dependency. |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | @tanstack/react-virtual npm package | low | accept | Legitimacy audit in `2-RESEARCH.md` confirms 22.4M weekly downloads, 4.5 yrs old, real `github.com/TanStack/virtual` repo, no `postinstall` script, React 18 supported. No network/storage/permission surface. |
| T-02-SC | Tampering | npm installs | high | mitigate | Package-legitimacy gate satisfied: audit table present in `2-RESEARCH.md`, package verdict `[OK]`/approved, no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages. No blocking human checkpoint required. |

## Verification

- **Per task:** `bun run test` (unit) + `bunx playwright test tests/e2e/dashboard.spec.ts` (e2e)
- **Per wave:** `bun run test:all` (lint + typecheck + unit + e2e)
- **Phase gate:** Full suite green before `/gsd-verify-work`

## Success Criteria

- `@tanstack/react-virtual@^3.14.10` installed via Bun; both feeds virtualized by row
- Only visible rows in the DOM with large datasets (bounded node count)
- Empty states, sort order, card styling, and interaction behavior preserved
- `MarketOdds`/`CorrelationPanel` untouched (D-02); no extra memoization or App.tsx state splitting (D-03)
- Unit + e2e tests green

## Output

Create `.planning/phases/02-ui-responsiveness/02-01-SUMMARY.md` when done.
